# {模块名} 模块结构文档

> 生成时间：{YYYY-MM-DD}
> 所属仓：{仓库名}
> 模块路径：{相对输入路径的路径}

## 模块职责

{2~4 句职责描述，基于目录名 + 抽样文件顶部注释 + 导出符号类型推断；读不到写"未识别"}

## 目录构成

| 子目录/关键文件 | 说明 |
| --- | --- |
| {子目录名或关键文件名} | {1 句说明，基于抽样} |
| ... | ... |

## 对外依赖

| 依赖模块 | 证据（import 所在文件路径） |
| --- | --- |
| {模块名} | {文件路径，多个用逗号分隔} |
| ... | ... |

## 被依赖

| 依赖方模块 | 证据（import 所在文件路径） |
| --- | --- |
| {模块名} | {文件路径，多个用逗号分隔} |
| ... | ... |
