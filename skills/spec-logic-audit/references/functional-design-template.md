# 功能实现设计

## 1 功能编号：XXX 功能实现（新增）

### 1.1 功能概述

本章节对实现该功能的背景和目的进行概述。

### 1.2 SR设计

在系统需求分析时，需要把SR关联到功能上，并承载在IT系统中。在功能设计时，可以通过IT系统导出本功能相关的SR清单。

| 系统需求编号 | 系统需求 | 系统需求描述 |
| --- | --- | --- |
| | | |

功能设计说明书是跨版本维护的，并且可以基于版本生成对应的数字化文档，本章节默认导出所选择版本的增量SR，但可以选择查看历史版本的SR清单。

本章节可对本版本的SR进行设计，描述SR的实现方式，对已有功能设计的修改，以及如何分解到实现元素。本章节主要为增量的设计内容，如对实现设计中的全量设计内容有修改，应同步刷新对应的内容。

部分SR关联了安全SPEI（安全领域策略使能项）实例，可在本章节对这类SR如何设计实现进行说明：

简单的、相对独立的安全实例化设计可以在本章节进行增量描述。比如：对"使用xxx强度的密码算法"的要求，可在本章节增加"使用公司xx加密组件"的设计描述。然后在安全配置设计、DFX分析章节产生相关的功能规格和AR即可。

与功能实现设计耦合密切的安全实例化设计则需要结合功能实现设计、接口设计、规格设计来开展，可在本章节简述增量设计思路，在实现设计、用户接口设计章节分别进行实现设计的描述并被本章节引用，然后在DFX分析章节产生相关的AR。比如：对于"密钥发送方和接收方进行双向身份认证"的要求，需要在实现设计中相关的用例中增加密钥收发相关的系统元素的交互、需要设计密钥收发认证接口、并根据设计可能需形成功能规格。

安全实例化设计可参考安全参考设计，以及SPEI实例关联的FR、FD、安全技术货架组件等安全设计资产。对于FD也需要进行实例化的设计；对于安全技术货架组件的使用，需要进行必要的集成设计。

本章节参考的安全实例化设计描述如下（样例以表格描述，产品可根据实际情况以子章节描述）：

| 系统需求编号 | 系统需求 | SPEI实例描述 | 实例化设计 |
| --- | --- | --- | --- |
| SRxxxx1 | 采用足够强调的加密算法 | 使用xxx强度的密码算法 | 使用公司xx加密组件 |
| SRxxx2 | 密钥收发进行双向认证 | 密钥发送方和接收方进行双向身份认证 | 调用密钥中心的密钥发放接口获取密钥，并使用HTTPS接口进行双向认证。详细设计参考实现设计相关章节 |

### 1.3 实现思路（可选）

本章节对如下内容进行简要描述：

1）实现该功能时所采用的思路、技术方法；

2）如果对该功能存在不同的技术方案，可以在此处描述所有备选方案，并对为何选择最终方案进行描述，以方便理解设计思路和后续回溯。功能具体实现部分只需描述最终方案。

本章节描述的是持续积累、修订的全量的设计思路。

### 1.4 实现设计

本章描述功能的具体实现方式，应当描述对功能输入所执行的所有操作，以及产生的对应输出，对功能规格和约束进行设计。建议根据功能的复杂程度，选择采用自然语言、时序图、状态机（算法）等适合的方式。

对功能处理和交互过程进行描述时，要注意具体交互的确切次序，以及各种事件的时序；描述的时候要以基本流程为主，并根据需要补充描述分支流程和异常处理，必要的时候可以再细分子流程，子流程可被基本流程、分支流程、异常处理流程调用。

采用自然语言进行描述时，可以首先说明这些处理是分别在哪个系统元素进行的，以便于以后整理得到系统元素规格，建议每个系统元素的一个处理过程分为一个小段描写。将来每一小段都是某一个系统元素的一条分配需求，容易进行规格列表的整理。同理如果采用自然语言对时序图等进行辅助描述时，亦可采用同样的处理方式。

在功能设计过程中，也需要将和功能相关的非功能需求，伴随着功能的分析过程分配到各个系统元素中，例如，当某一个功能要求某个系统元素处理某种类型的消息时，那么同时也可以提出相关的性能需求，比如，在同一时间最多处理200个此类型的消息。又比如，某个系统元素需要保存相关的一些信息到硬盘上，那么也可以提出相关的性能需求：一次信息处理和存储完成的时间不超过5秒。等等。

另外，如果要限定系统元素内部的实现方式时，也可以提出一些设计约束，比如建议采用的物理器件，具体的算法要求，限定的实现接口类型等。

功能定义中的输入、处理、输出、规格、约束，需要有对应的设计内容与之对应，以确保设计完整，如何具体表达对应的映射关系不做限制。

时序图、活动图或者其他描述方式对功能实现开展的设计，与系统用例类似，都是通过一系列的交互完成特定的任务。与系统用例不同的是，在功能设计中，需要将系统展开，体现更细粒度的交互过程（如时序图一般展开到架构元素，活动图展开到子活动，等等），本文将这些设计内容称为设计用例，采用独立的章节进行描述，章节名称为xxx用例设计。

功能实现设计中，进行安全实例化设计的说明：

不能简单的进行增量设计描述的安全实例化设计，需要在本章节进行描述。如果上述安全设计和功能实现设计耦合密切，可以合并到功能的用例设计中共同进行。如果安全设计相对独立，可以进行独立的设计，以独立的章节承载。

在这个过程中，针对量化的、能力型的设计结果，可以形成条目化的功能规格，刷新到功能树中。同时，设计需要分解到架构元素（采用序列图等方式），形成AR往下游传递。

在进行设计时，可参考安全参考设计，以及SPEI实例关联的FR、FD、安全技术货架组件等安全设计资产。对于FD需要进行实例化的设计；对于安全技术货架组件的使用，需要进行集成设计。

#### 1.4.1 Xxx用例设计-时序图（可选）

用例编号：（给设计用例进行唯一编号）

时序图描述方式（通过时序图等描述时，亦可通过自然语言描述进行辅助）：

【此处插入时序图】

说明：

1）采用时序图方式进行设计说明时，时序图中的生命线建议调用架构设计的逻辑元素或者实现元素，不同架构元素之间的交互，也建议调用架构设计的逻辑接口或者实现接口。如果功能设计涉及的元素、接口在架构设计中不存在，则建议应该先刷新架构设计的内容。

2）生命线上的活动承载的是该逻辑元素已实现、新增或修改的行为。这部分内容，建议作为系统实现元素规格（如果生命线上是逻辑元素，则需要将逻辑元素的规格转换为实现元素的规格）。

#### 1.4.2 Yyy用例设计-时序图（可选）

xxx

#### 1.4.3 Xxx用例设计-活动图（可选）

xxx

#### 1.4.4 Xxx用例设计-状态机（可选）

xxx

#### 1.4.5 Xxx用例设计-自然语言描述（可选）

用例编号：（给设计用例进行唯一编号）

前置条件：XXX

触发事件：XXX

主流程描述X（可以有多个主流程，亦可有备选流程、异常流程，描述方法相同）：

流程描述：

- 步骤1：；
- 步骤2：；
- 步骤3：；
- 步骤4：；

后置条件：Xxx；

### 1.5 用户接口设计

判断是否涉及对外接口变化。如果涉及，对系统对外接口包括OM接口，进行实现方案设计，其中OM接口设计内容包括：

1. 此OM接口作用和应用场景说明
2. 接口使用的前提条件（例如是否有权限要求，是否需要先完成其他配置等）
3. 接口使用的约束和限制（例如xx场景不支持该接口等信息）
4. 基于数字化工具，输出告警、性能指标、OM操作接口和系统参数的数字化对象定义。

### 1.6 实现接口设计

#### 1.6.1 接口设计

对系统元素进行实现接口设计，接口定义到接口名称、接口职责、接口输入输出要求，产品按需要求是否定义到具体参数。

以下是示意描述模板，产品按需修订采用，建议使用工具管理。

| | |
| --- | --- |
| 接口名称 | |
| 接口描述 | |
| 接口类型 | |
| 所属系统元素 | |
| 归属的逻辑接口名 | ITF_XX_CFG |
| Input要求/参数（可选） | 产品自己判断是仅输出接口要求，还是输出具体参数 |
| Output要求/参数（可选） | |
| 规格、约束和注意事项 | |
| 版本变化 | V3R1新增 |
| 接口源码文件（系统级接口可选） | TR3不要求。基于接口管理工具进行接口资产管理的，通过工具来维护或检查一致性，新增接口TR5要完成。 |

#### 1.6.2 接口权限设计（可选）

对对外暴露接口（含生态接口）做清单化管理，并开展权限设计，在架构设计中承载公共的权限模型/机制设计。

**示例1（GTS）：**

1. 角色：

全量角色清单在架构设计中承载，本章节描述各角色是否有权限操作本接口。

| 角色名称 | 是否有权限操作本接口 |
| --- | --- |
| Admin | √ |
| 租户 | √ |
| Guest | X |

2. 功能权限：

权限控制模型/机制在架构设计中承载，本章节基于选取的权限控制模型/机制对接口的功能权限进行描述。

- 页面权限：支持管理员、租户数据查询。
- 操作权限：租户对数据增加、修改、删除，管理员进行审核操作。

3. 资源权限：

权限控制模型/机制在架构设计中承载，本章节基于选取的权限控制模型/机制对接口的资源权限进行描述。

- 只有租户可以访问本接口，不同租户的资源隔离。

| 组织维度 | 客户维度 | 行业维度 | 其他维度 |
| --- | --- | --- | --- |
| X | 租户 | X | X |

**示例2（终端）：**

对提供的接口进行权限访问控制，如果不涉及提供接口该章节写不涉及，删除模板内容。如果按照此表格模板涉及接口权限管控设计，则打√，并展开进行设计。

| 提供接口 | 可见性范围 | 可用性范围 | 敏感资源管控指导 | 涉及（打√） |
| --- | --- | --- | --- | --- |
| xx接口 | 向三方应用开放 | 完全开放（包括三方应用）：涉及敏感数据/资源调用 | 采用ohos.permission.xxx权限管控方式 | |
| xx接口 | 向三方应用开放 | 仅给部分三方应用开放 | 采用ohos.permission.xxx权限管控方式 | |
| xx接口 | 向三方应用开放 | 仅给单个三方应用开放 | 可使用两种方式（选择其中一种即可）：1. 采用ohos.permission.xxx权限管控方式 2. 采用AppIdentifier + bundleName方式（不推荐） | |
| xx接口 | 仅向系统应用开放 | 给所有系统应用开放 | 采用systemAPI管控 | |
| xx接口 | 仅向系统应用开放 | 仅给部分系统应用开放 | 采用systemAPI管控 + ohos.permission.xxx权限管控方式 | |
| xx接口 | 仅向系统应用开放 | 仅给单个系统应用开放 | 可使用三种方式：1. 采用systemAPI管控 + ohos.permission.xxx权限管控方式 2. 采用systemAPI 和 bundleName管控方式 3. 采用systemAPI管控 + (AppIdentifier 和 bundleName)方式 | |
| xx接口 | 仅向SA开放 | 给所有系统服务开放 | 采用tokentype管控方式 | |
| xx接口 | 仅向SA开放 | 仅给部分系统服务开放 | 采用tokentype管控 + ohos.permission.xxx权限管控方式 | |
| xx接口 | 仅向SA开放 | 仅给单个系统服务开放 | 可使用三种方式：1. 采用tokentype管控 + ohos.permission.xxx权限管控方式 2. 采用tokentype管控 + UID管控方式 3. 采用tokentype管控 + processName管控方式 | |

### 1.7 安全配置设计

本章节为功能设计的安全配置，识别安全配置项，定义/刷新安全配置规则，形成产品版本安全配置基线，包括输出应用层安全配置和刷新公共组件安全配置。分两种情况：

1）产品有OM设计，安全配置设计在OM设计基础上开展，结合OM设计的配置设计上进行。

2）产品无OM设计，安全配置设计在接口设计之后开展，接口设计完成系统对外接口，在对外接口基础上分析，是否有配置相关接口，再进行安全配置设计。

#### 1.7.1 识别安全配置项

应用层安全配置项在功能实现设计、接口设计（含外部接口）基础上识别安全配置项。依据《安全配置设计指导书》识别安全配置项，通常功能中涉及到配置且为安全功能的，需要进行安全配置设计；还有一些功能有安全属性且安全属性需要配置的，需要进行安全配置设计；另外一些功能是来自客户需求且有安全配置要求的，需要进行安全配置设计。

识别安全配置项后输出（可使用工具进行作业）：

| 功能域/功能 | 是否有安全配置项 | OM对象/外部接口（可选） | 安全配置项说明 |
| --- | --- | --- | --- |
| 功能域/功能名称 | 是/否 | 1、有OM对象则给出/定义OM对象名称，如果没有则为空，OM对象关联外部接口。2、有外部接口，给出外部接口命令字。 | 给出需要有安全配置项的说明 |
| 例如：SNMP协议配置 | 是 | SNMP协议代理，关联SNMP协议配置命令字。SNMP协议配置接口（命令字） | SNMP协议的端口、版本等可配置，存在安全风险，需要有安全配置项。 |
| | | | |

公共组件安全配置项在"架构设计"中已给出初稿，这里根据需要进行刷新。

#### 1.7.2 安全配置规则定义

针对已识别的安全配置项，开展安全配置规则设计（定义或刷新已有安全配置规则），输出安全配置规则。

应用层安全配置设计，对于需要有安全配置项的功能域或功能。1）有OM设计场景，如有OM对象则结合OM对象定义安全配置规则，如没有OM对象，若需要则可创建OM对象再定义安全配置规则，如不需要则直接定义安全配置规则。2）无OM设计，在接口设计基础上开展，根据外部接口进行判断，在配置接口基础上定义安全配置规则。定义安全配置规则可使用工具进行作业；

1）指定功能或功能域；

2）OM对象（可选），如有OM对象则选择OM对象，如无OM对象，需要则创建OM对象明确OM对象关系属性，不需要则不创建。OM对象/外部接口给出关键参数的参数类型、取值范围、缺省值、建议值、初始值来源、影响范围、与其它参数关系等。

3）定义/刷新安全配置规则，结合SR、功能实现设计设计安全配置规则。1）公共组件的安全配置规则基于公共组件安全配置基线进行定义/刷新。2）应用层安全配置规则基于应用安全配置基线进行定义/刷新。建议采用工具作业，定义/刷新安全配置基线（最新属性在工具上更新）。

**应用层安全配置规则示例：**

| 项目 | 安全配置规则1 | 安全配置规则2 | 安全配置规则3 |
| --- | --- | --- | --- |
| 所属功能/功能域 | 用户管理 | 证书管理 | 日志管理 |
| OM对象/命令字（可选） | PWDPOLICY | CA | LOGPOLICY |
| 配置参数（可选） | COMPLICACY | SIGNALG | ENCRYPTSTORESW |
| 规则分类（支持定制） | 基站OM安全 | 基站传输安全 | 基站设备安全 |
| 规则子类（支持定制） | 本地安全策略管理信息 | 证书签发 | 日志管理策略 |
| 规则子类ID | 系统自动分配 | 系统自动分配 | 系统自动分配 |
| 规则名称 | 确保口令策略配置了复杂度检查 | 确保数字证书配置了安全的签名算法 | 确保日志本地加密存储开关配置为开启状态 |
| 规则ID | 102021 | 201011 | 407011 |
| 风险等级 | 高 | 高 | 高 |
| 规则描述 | 该参数表示密码的最小复杂度，可通过该参数设置密码中必须包含的字符类别来约束设置的密码满足一定复杂度要求 | 参数SIGNALG的名称为签名算法，表示CMP消息使用的签名算法 | 该参数表示是否对基站本地存储的操作日志和安全日志进行加密保护 |
| 风险描述 | 复杂度低的密码更容易被暴力破解，从而导致账号被盗用 | 使用不安全的签名算法，如SHA1算法存在安全缺陷，可能导致证书伪造 | 当ENCRYPTSTORESW设置为OFF时，禁用本地存储日志的加密，增加信息泄露的风险 |
| 修复影响 | （1）密码越复杂，密码强度越强。（2）若全网采用统一的密码策略，需要同步修改其他设备的策略。（3）该参数修改后，当添加用户或修改现有用户的密码时生效 | （1）如果证书服务器不支持指定的值，或者证书服务器的配置没有同时更改，则证书申请和更新失败。（2）该参数对下次证书申请和更新生效，但不影响现有证书 | （1）修改后的参数值仅在主控板为UMPT或UMDU时生效。（2）加解密需要一段时间，影响日志查询或上传。（3）该参数修改后立即生效 |
| 检查方式 | 检查系统密码策略，查看密码复杂度的配置是否在安全推荐值范围内 | 检查证书签发使用的签名算法，查看签名算法是否在安全推荐值范围内 | 检查日志本地加密存储开关是否配置为开启状态 |
| 取值范围 | 依照实际取值范围填写 | 依照实际取值范围填写 | [ON，OFF] |
| 安全推荐值 | LOWERCASE-1&DIGIT-1&UPPERCASE-1&SPECHAR-1 | SHA256,SHA384,SM3 | ON |
| 缺省值 | LOWERCASE-1&DIGIT-1&UPPERCASE-1&SPECHAR-1 | SHA256 | ON |
| 操作说明/修复建议 | 建议通过SET PWDPOLICY命令将COMPLICACY设置为必须包含小写字母、大写字母、数字、特殊字符 | 建议通过MOD CA命令将SIGNALG设置为SHA256或高级算法 | 建议通过SET LOGPOLICY命令将ENCRYPTSTORESW设置为ON |
| 是否必选项 | 是 | 是 | 是 |
| 是否默认安全 | 是 | 是 | 是 |
| 扩展字段（自定义） | NA | NA | NA |

应用层安全配置规则作为特定功能规格需要下游开发时，应提取AR给网元、巡检工具，通过AR跟踪落地安全配置规则。

**公共组件安全配置规则示例：**

| 项目 | 安全配置规则1 | 安全配置规则2 | 安全配置规则3 |
| --- | --- | --- | --- |
| 所属系统元素 | 操作系统 | 数据库 | Tomcat组件 |
| 公共组件族 | OS族 | 数据库 | WEB |
| 公共组件类 | RTOS | GaussDB | Tomcat |
| 基线名称 | RTOS 208 安全配置基线 | GaussDB（100）V3安全配置基线 | Tomcat 9 安全配置基线 |
| 基线版本 | V1.0 | V1.0 | V1.1 |
| 规则分类 | 用户账户与环境 | 用户安全策略 | 初始配置 |
| 规则子类（支持定制） | | | |
| 规则子类ID | C.OS.RTOS.G_2 | C.DB.GAUSSDB.G_3 | C.WEB.TOMCAT.G_1 |
| 规则名称 | 确保账号策略配置了连续3次输入错误口令后锁定策略 | 确保账号密码配置了有效期 | 确保更改默认shutdown端口号和命令 |
| 规则ID | C.OS.RTOS.O_208.G_2.R_5 | C.DB.GAUSSDB.O_3.G_3.R_6 | C.WEB.TOMCAT.O_9.G_1.R_6 |
| 风险等级 | 高 | 中 | 高 |
| 规则描述 | 需要设置用户的失败登录重试次数，当用户连续失败登录达到阈值时，要对用户进行锁定 | 密码有效期由参数PASSWORD_LIFE_TIME指定，用于设置同一密码所允许使用的天数 | Tomcat默认会监听8005端口上的shutdown请求，容易被利用关闭服务器 |
| 风险描述 | 攻击者在对目标进行攻击时，最常用的手段是不断进行登录尝试，如果不限制登陆尝试次数，攻击者能不断进行登录尝试爆破用户口令 | 账号密码未配置有效期，导致密码长期有效，增加了密码泄露风险 | Tomcat服务器提供默认shutdown端口和关闭命令，很容易被黑客利用关闭服务器 |
| 修复影响 | 被锁定账号不能正常登录，需管理员手动解锁或等待锁定时长 | 配置用户有效期后，当用户不在有效期限范围内时，将无法与数据库建立连接 | 无 |
| 检查方式 | 在/etc/pam.d/common-auth文件中检查配置情况 | 执行SQL查询密码有效期配置 | 检查server.xml是否使用默认的shutdown命令和端口 |
| 取值范围 | deny=[0,正整数] unlock_time=[0,正整数] | PASSWORD_LIFE_TIME：(0, 24856), unlimited | shutdown端口号：1024-65536 shutdown命令：任意字符组合 |
| 安全推荐值 | deny=3, unlock_time=[300-600] | PASSWORD_LIFE_TIME：(0, 90] | shutdown端口号：非8005 shutdown命令：大于8位的任意字符串 |
| 缺省值 | deny=0 unlock_time=300 | PASSWORD_LIFE_TIME：180 | shutdown端口号：8005 shutdown命令：SHUTDOWN |
| 操作说明/修复建议 | 修改/etc/pam.d/common-auth文件中deny和unlock_time字段 | ALTER PROFILE profile_name LIMIT PASSWORD_LIFE_TIME 90 | 更改server.xml中shutdown端口和命令 |
| 是否必选项 | 是 | 是 | 是 |
| 是否默认安全 | 是 | 是 | 是 |
| 扩展字段（自定义） | NA | NA | NA |

公共组件安全配置规则作为特定功能规格需要下游开发时，应提取AR给网元、巡检工具，通过AR跟踪落地安全配置规则。

### 1.8 功能规格变更与设计（可选）

本章节对功能规格进行变更与设计。

#### 1.8.1 功能规格变更

在功能实现设计过程中，可能会新增或修改功能规格，本章节即对本功能下的功能规格进行刷新。

功能规格变更时，需要检查对应功能的可验证性策略是否需要调整。

例如：在分析过程中识别到需要补充可靠性规格，则在本章节对新增的可靠性规格进行说明。

| 规格名称 | 规格描述 |
| --- | --- |
| 主备切换时长 | XXX进程在检测到主节点故障后，必须在60秒内完成主备切换，解决网络脑裂导致的服务中断问题 |

#### 1.8.2 功能规格1分解

【参考信息】功能规格是功能的重要组成部分，需要针对功能规格如何实现进行设计，即按需将功能规格要素分配到支撑功能实现的架构元素上。如果前面功能实现设计已经完成的，则不需要重复设计，但是结果建议统一呈现。

##### 1.8.2.1 功能规格1设计（可选）

【关键信息】按需给出功能规格1的设计思路或者设计方案，可选。

##### 1.8.2.2 功能规格1分解

【关键信息】结构化输出功能规格1分解结果，某些规格分解时，对于一些架构元素没有显式的规格要求，则注明无特殊要求，此处新分解到架构元素规格需要生成AR。

| 功能规格名称 | 功能规格描述 | 实现元素名称 | 规格分解描述 |
| --- | --- | --- | --- |
| 功能1名称 | 功能规格1描述 | 实现元素1 | 支持配置的客户端地址数量 <=1024 |
| 功能1名称 | 功能规格1描述 | 实现元素2 | 无特殊要求 |
| 功能1名称 | 功能规格1描述 | 实现元素3 | IOPS 不低于100K |

#### 1.8.3 功能规格2分解

##### 1.8.3.1 功能规格2设计（可选）

【关键信息】按需给出功能规格2的设计思路或者设计方案，可选。例如：

- 设计思路：基于"主备冗余"模式，采用双节点反亲和部署，通过ETCD-HA进程实现多路径心跳检测、仲裁及切换控制。
- 功能点描述：主备切换流程，在检测到主节点故障后，60秒内完成备节点升主并接管服务。
- 输入：故障检测结果（功能点2输出）；备节点健康状态。
- 处理：ETCD-HA触发备节点升主；备节点接管VIP，更新服务注册信息；主节点恢复后，保持备状态或根据策略重新仲裁。
- 输出：切换完成时间戳（≤60秒）。

##### 1.8.3.2 功能规格2分解

【关键信息】结构化输出功能规格2分解结果，某些规格分解时，对于一些架构元素没有显式的规格要求，则注明无特殊要求，此处新分解到架构元素规格需要生成AR。

| 功能规格名称 | 功能规格描述 | 实现元素名称 | 规格分解描述 |
| --- | --- | --- | --- |
| 主备切换时长 | XXX进程在检测到主节点故障后，必须在60秒内完成主备切换 | 实现元素1 | 支持XXX节点主备切换进程调度周期＜60s |
| 主备切换时长 | XXX进程在检测到主节点故障后，必须在60秒内完成主备切换 | 实现元素2 | 无特殊要求 |

#### 1.8.4 功能规格n分解

##### 1.8.4.1 功能规格n设计（可选）

【关键信息】按需给出功能规格n的设计思路或者设计方案，可选。

##### 1.8.4.2 功能规格n分解

【关键信息】结构化输出功能规格n分解结果，某些规格分解时，对于一些架构元素没有显式的规格要求，则注明无特殊要求，此处新分解到架构元素规格需要生成AR。

| 功能规格名称 | 功能规格描述 | 实现元素名称 | 规格分解描述 |
| --- | --- | --- | --- |
| 功能n名称 | 功能规格n描述 | 实现元素1 | 支持配置的客户端地址数量 <=1024 |
| 功能n名称 | 功能规格n描述 | 实现元素1 | 5s内可检测故障 |
| 功能n名称 | 功能规格n描述 | 实现元素2 | IOPS 不低于100K |

### 1.9 DFX分析

考虑到某些DFX要求和约束，和功能的具体实现方案相关，因此需要在完成功能设计后，再进行一次分析和检查。如果识别了新的改进要求，则去迭代调整"功能实现设计"章节的设计，本章节只分析识别问题，不描述具体的设计实现方案。

当前只针对可靠性和安全设计识别了Check的具体方案，其他DFX待相关的Owner重新进行审视后补充。产品如果识别需要对其他DFX进行功能实现设计后的Check，则可以自行补充相关子章节。

#### 1.9.1 可靠性分析

功能的可靠性分析是通过功能FMEA来开展，对功能的处理流程进行分析，明确在各种异常情况出现时如何保证功能的可靠性，是站在最终用户的角度分析功能的主要实现步骤和方案的完善和完备性。

分析的内容包括以下几个方面：

1）分析功能，该功能相关的系统元素以及系统元素间的交互过程中（可基于功能的顺序图开展）存在哪些故障模式。

2）分析每个故障模式出现情况下对系统所产生的影响及严酷度。

3）分析引发此故障模式的故障原因。

4）针对故障模式、故障原因或故障影响，提出可行的针对性的改进措施，并注明相应改进措施是否当前已经支持。

5）对于当前版本尚未支持的改进措施，开展设计权衡和决策，对需实现的改进措施，分配到相应的系统元素，形成AR。

分析模板如下：

【FMEA分析表格模板，按需采用工具管理】

#### 1.9.2 可靠性改进措施设计（可选）

功能设计阶段，需要对FMEA分析得到的复杂改进措施进行详细设计，例如：

- 改进措施：网络断链情况下重试机制
- 实现思路：采用有限重试模式实现网络断链场景下的容错机制。当连续3次健康检查失败时触发故障隔离，将故障服务从节点池移除并持续检测恢复状态。恢复后通过健康检查验证重新加入可用节点池，确保系统在故障自愈后快速恢复服务能力。同时记录关键故障事件日志，支持后续分析。
- 功能点描述：维护连续失败计数器，当达到阈值时触发隔离机制
- 输入：当前重试计数（初始值0）；健康检查结果（成功/失败）
- 处理：成功时重置计数器；失败时递增计数，超过3次时触发隔离
- 输出：是否触发隔离指令（布尔值）；当前计数器值

#### 1.9.3 安全检查

功能阶段主要开展安全检查，并对设计结果确认，具体如下：

1）在"功能实现设计"活动中开展安全设计，包括安全类功能设计、系统级的安全韧性系统需求设计。通过设计，从各类SR中识别出安全需求并分配到系统元素，形成安全AR。

- 安全SR跟随其它SR进入设计过程。
- 如同其它DFx需求一样，我们需要在设计过程中，综合考虑功能性需求，安全需求和其它DFx需求，进行综合的优化设计。必要时，需要进行多轮迭代，从而实现功能的最优设计。
- 在安全SR向安全AR的转换过程中，如果必要，可以结合功能的实现情况，再次识别潜在的攻击方式和潜在的可被利用的漏洞。

2）设计完成，生成AR清单后，应该对涉及到的架构元素，基于"威胁模式库"，进行"安全设计确认"，确保所有已识别的安全处置措施都已经落地。

3）针对机密数据，检查敏感操作。对机密数据的不恰当处理会直接导致安全泄露风险。为了避免主动的安全泄露风险，我们需要根据"高风险操作模式库"的提示，识别功能相关的架构元素中是否对机密数据执行了高风险的处理，并进行恰当的处置。

##### 1.9.3.1 安全设计确认

设计完成，生成AR清单后，应该对涉及到的架构元素，基于"威胁模式库"，进行"安全设计确认"，主要目的包括两个：

1）确认受影响的架构元素，必要的安全处置措施已经落地；否则，应该增加对应的AR。

2）确认安全AR相关的安全处置措施，已经被"威胁模式库"所包含；否则，应该增加到威胁模式库中。

确认方法：

1）识别受影响的架构元素；功能设计过程中，部分架构元素的功能以及接口会进行修改。基于AR清单进行识别；

2）对受影响的架构元素，根据AR内容，检查处置措施是否落地；如果未落地，则应该增加对应的AR；

3）对安全AR，识别处置措施是否被威胁模式库所包含；如果未包含，则应该增加相关内容。

##### 1.9.3.2 敏感操作检查

参考红线定义，机密数据的具体范围取决于系统具体的应用场景，设计师应根据风险进行分析和判断。典型的机密数据包括认证凭据（如口令、动态令牌卡）、银行帐号、大批量个人数据和用户通信内容等。

**机密数据清单：**

| 数据字段 | Data Field | 备注/说明 | 数据字段敏感度 | 关联架构元素 | 强制的操作 | 禁止的操作 |
| --- | --- | --- | --- | --- | --- | --- |
| 管理员账号/密码 | Administrator Account/Password | 系统管理员的账号和密码 | 高 | 本地认证 WEB管理 | 加密传输 加密存储 匿名化 | 回显 日志 |
| | | | | | | |
| | | | | | | |

"机密数据"应该进行正式的清单化管理，归档在4+1视图的"安全数据模型"中。同步进行"敏感数据"的识别，同样进行清单化管理。"敏感数据"归档在4+1视图的"隐私数据模型"中。

在完成设计后，增加对"机密数据"的敏感操作的检查，作为对威胁建模结果的检验，同时最大可能的避免信息泄露风险。

对于机密数据，从两个维度对其进行分析：

1）生命周期维度：对于识别出的机密数据，需要完整地识别出数据的生命周期，识别其"产生、使用、传输、持久化和销毁"的过程，以避免后续风险识别过程中无意的疏漏。

2）高风险处理过程：识别对机密数据的处理过程中，是否有高风险的处理。典型的高风险处理包括："日志/Debug信息"，"回显"，"缓存"，"硬编码"，以及"编码/复制/不安全算法"。

**高风险操作模式库：**

| | 产生 | 使用 | 传输 | 持久化 | 销毁 |
| --- | --- | --- | --- | --- | --- |
| 日志/Debug信息 | | | | | |
| 回显 | | | | | |
| 缓存 | | | | | |
| 硬编码 | | | | | |
| 编码/复制/不安全算法 | | | | | |

为了避免主动的安全泄露风险，需要根据"高风险操作模式库"的提示，识别架构元素中是否对机密数据执行了高风险的处理。如果实施了，则应该进行记录，并进行消减。

##### 1.9.3.3 安全技术货架组件清单（可选）

功能设计之后，需对功能设计中使用的安全技术货架组件情况进行梳理和汇总，以便下游开展软件设计、测试验证。如果产品未使用安全技术货架组件，则本章节可裁剪。

| 组件交付件名称 | 版本 | 用途 | 使用说明（可选） |
| --- | --- | --- | --- |
| HiSec-ICT TLS Crypto | V15.0 | 加解密 | 库文件调用 |
| … | | | |

### 1.10 分配需求

将系统需求（SR）分配到系统元素，形成分配需求（AR），可以直接承载在IT系统中。此处是本功能下的历史系统需求（SR）和分配需求（AR）的汇总，汇总时需要显示版本信息。

| 系统需求编号 | 系统需求 | 分配需求编号 | 分配需求描述 | 系统元素 |
| --- | --- | --- | --- | --- |
| | | | | |
| | | | | |