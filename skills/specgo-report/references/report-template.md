# {功能名} 需求到代码全链路分析报告

| 元信息 | 值 |
| --- | --- |
| 需求 | {需求/SR 文档路径或名称} |
| 分支 | {分支名}（{YYYY-MM-DD}） |
| 生成日期 | {YYYY-MM-DD} |
| Skill | specgo-report（specgo 第 6 步调度） |
| 流程执行 | 资产{已录入/已刷新/跳过} → 需求审核{通过/跳过} → story 设计{已产出} → 代码实现{已完成} → 资产刷新{已刷新/待确认/跳过} |

## 一、测试用例执行结果

<!-- 以实跑证据为唯一事实来源（验证命令输出逐条核对），禁止凭印象；DT 到测试函数级、集成到用例步骤级；失败用例必须列出并附处理结论；环境前置（数据基线/服务依赖/环境变量）必须写明，前置不满足导致的失败要显式标注 -->

### DT 单元测试（{YYYY-MM-DD} 实跑）

验证命令：`{构建/测试命令}`

| 包 | 结果（PASS/FAIL 数） | 本需求相关用例 |
| --- | --- | --- |
| {src/...} | {N PASS / M FAIL} | {测试函数名（一句话看护点），逐条列} |

### 集成测试（{YYYY-MM-DD} 实跑，{环境前置}；仓内无集成/E2E 约定时写"无集成测试约定，跳过"）

| 用例 | 步骤数 | 结果 | 覆盖需求点 |
| --- | --- | --- | --- |
| {testsuit/...} | {S1~Sn} | {n/n PASS} | {该用例看护的需求点/裁定条目} |

## 二、代码修改清单

<!-- 以 git diff 为唯一事实来源；每行一个"文件 × 函数"粒度；变更说明≤30 字，禁止粘贴代码片段与设计细节；「业务规则」列填该改动实现/对齐的规则条目（优先 docs/0-biz/rules/ 既有条目名，story/裁定新增规则注明来源），测试代码填其看护的规则，清单外改动与产物填「清单外（无规则）」；清单外改动/产物必须列入并标注 -->

| 文件 | 新增/修改函数 | 变更说明（≤30 字） | 业务规则 |
| --- | --- | --- | --- |
| {src/...} | {修改 `funcName`} | {一句话} | {规则条目名（来源）} |

## 三、变更框图与引用资产质量评估

<!-- 先图后推理：用一张 mermaid flowchart 把本次代码修改逻辑串联成变更框图——按调用链路（入口 → controller → service → dao/缓存 → 存储）布点，变更节点在 label 中标注"变更：xxx"并统一配色；图下针对图中每一处变更给推理：变更内容一行 + 引用文档四列表格（引用文档 / 应用了文档中的什么内容（≤30 字）/ 文档质量评分（符合/部分符合/不符合）/ 文档优化建议（无则写"无"））+ 结论一行；测试代码不单独给推理，并入其看护的变更点；重点呈现缺口——缺引用（无资产支撑、全靠读代码反推）与质量不足（评分低于"符合"），缺口必须在优化建议列指回具体 skill 重跑或文档修订 -->

```mermaid
flowchart LR
    A1["{入口}"] --> A2["{controller.func}<br/>变更：{一句话}"]
    A2 --> A3["{service.func}<br/>变更：{一句话}"]
    A3 --> A4[("{存储/缓存}")]
    classDef chg fill:#ffe4b5,stroke:#d4930d;
    class A2,A3 chg;
```

### 变更 1：{函数} —— {业务规则}

变更内容：{一句话}（证据：{文件路径}）

| 引用文档 | 应用内容（≤30 字） | 质量评分 | 优化建议 |
| --- | --- | --- | --- |
| {docs/0-biz/interface/interface-auth.md} | {响应契约三档 code 取值} | {符合} | {无} |
| {docs/0-biz/interface/interface-login.md} | {Q2 范围边界：登录链路是否同改} | {部分符合} | {补记 deviceLoginAuth 注入现状} |

结论：{无缺口 / 缺引用 / 引用质量不足——依据}

### 变更 2：{……}

汇总：缺少文档引用的变更 {N} 处（{清单或"无"}）；引用质量不足的变更 {N} 处（{清单 + 补强指向}）。

<!-- 表后附注：生成中发现的"未引用但已失实/有缺口"的资产问题，一行一条，含补强建议；无则写"无" -->
