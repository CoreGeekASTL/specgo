# {功能名} 需求到代码全链路分析报告

| 元信息 | 值 |
| --- | --- |
| 需求 | {需求/SR 文档路径或名称} |
| 分支 | {分支名}（{YYYY-MM-DD}） |
| 生成日期 | {YYYY-MM-DD} |
| Skill | specgo |
| 流程执行 | 资产{已录入/已刷新/跳过} → 需求审核{通过/跳过} → story 设计{已产出} → 代码实现{已完成} → 资产刷新{已刷新/待确认/跳过} |

## 一、测试用例执行结果

<!-- 以实跑证据为唯一事实来源（验证命令输出逐条核对），禁止凭印象；DT 到测试函数级、集成到用例步骤级；失败用例必须列出并附处理结论；环境前置（数据基线/服务依赖/环境变量）必须写明，前置不满足导致的失败要显式标注 -->

### DT 单元测试（{YYYY-MM-DD} 实跑）

验证命令：`{构建/测试命令}`

| 包 | 结果（PASS/FAIL 数） | 本需求相关用例 |
| --- | --- | --- |
| {src/...} | {N PASS / M FAIL} | {测试函数名（一句话看护点），逐条列} |

### 集成测试（{YYYY-MM-DD} 实跑，{环境前置}；仓内无集成/E2E 约定时写"无集成测试约定，跳过"）

| 用例 | 步骤数 | 结果 | 覆盖需求点 |
| --- | --- | --- | --- |
| {testsuit/...} | {S1~Sn} | {n/n PASS} | {该用例看护的需求点/裁定条目} |

## 二、代码修改清单

<!-- 以 git diff 为唯一事实来源；给人看的一览表，每行一个"文件 × 函数"粒度；变更说明≤30 字，禁止粘贴代码片段与设计细节；清单外改动/产物必须列入并标注「清单外」 -->

| 文件 | 新增/修改函数 | 变更说明（≤30 字） |
| --- | --- | --- |
| {src/...} | {修改 `funcName` / 新增 N 个 Test 函数} | {一句话} |

## 三、引用资产质量评估（需求点 × 引用文档匹配）

<!-- 以需求点为主键：从 story（核心要素变更总览/各要素章节）与 develop-task（澄清问题列表/修改文件清单）提取全部需求点（含用户裁定条目），逐点匹配实现期实际引用的资产文档（story 外部文档引用 + develop-task 框架表 + 实现子代理返回的实际加载文档清单）；重点呈现缺口——缺引用（该需求点无任何资产支撑、全靠读代码反推）与质量不足（引用文档失实/缺记载/过期）；曾不足但本次已补强的标注"曾质量不足，已补强" -->

| 需求点 | 引用资产文档（质量：符合/部分符合/不符合） | 缺口判定与补强建议 |
| --- | --- | --- |
| {需求点} | {docs/...（质量）} | {无缺口 / 缺引用 / 引用质量不足——依据 + 指向具体 skill 重跑或文档修订} |

汇总：缺少文档引用的需求点 {N} 个（{清单或"无"}）；引用质量不足的需求点 {N} 个（{清单 + 补强指向}）。

<!-- 表后附注：生成中发现的"未引用但已失实/有缺口"的资产问题，一行一条，含补强建议；无则写"无" -->
