# 韧性规范（故障策略）

| 元信息 | 值 |
|--------|-----|
| 分支 | <分支名> 分支 (<YYYY-MM-DD>) |
| 更新日期 | <YYYY-MM-DD> |
| Skill | tech-resilience-guidelines-analyze |
| 运行模式 | 起草模式 |

## 扫描范围总览

| 点位类型 | 数量 | 说明 |
|---|---|---|
| 出站调用点 | <N> | <协议/目标构成，如 HTTP 12 处、RPC 5 处、MQ 生产 3 处、外部存储 4 处> |
| 后台任务 | <N> | <构成，如定时任务 2 个、消费循环 1 个、独立 worker 1 个> |

| 策略 | 已设置点位数 | 未设置/框架默认点位数 | 现状一句话 |
|---|---|---|---|
| 超时 | <N> | <N> | <如"超时值分散在调用点与封装层，取值 1s~30s 不一"> |
| 重试 | <N> | <N> | <如"仅 MQ 消费有重试，出站调用均无重试"> |
| 熔断与降级 | <N> | <N> | <如"全仓无熔断器，个别调用有默认值兜底"> |
| panic/recover 兜底 | <N> | <N> | <如"主入口有 recover，后台 goroutine 无兜底"> |
| 错误 swallowing | — | <N> | <吞错点位数与主要形态> |

## 一、超时策略

### 现状调用点分布

| 调用点（文件 · 函数） | 被调目标 | 超时值与位置 | 证据（文件路径） |
|---|---|---|---|
| <文件 · 函数> | <服务/中间件/topic> | <如 "3s，调用点显式设置" / "封装层统一 5s（封装层文件）" / "未设置（框架默认）"> | <文件路径，不带行号> |

### 应有约定建议

<基于现状分布的指导性约定：超时应在哪一层设置（建议统一封装层，禁止裸用无超时 client）、建议取值区间（按被调目标类型区分）、配置化要求（配置 key 命名）等；建议须从现状多数派实践与框架能力归纳，标注为建议而非现状>

## 二、重试策略

### 现状调用点分布

| 调用点（文件 · 函数） | 被调目标 | 重试次数/间隔/退避 | 重试触发条件 | 幂等保障 | 证据（文件路径） |
|---|---|---|---|---|---|
| <文件 · 函数> | <服务/中间件/topic> | <如 "3 次，固定间隔 1s" / "指数退避 100ms 起" / "无重试"> | <哪些错误/状态码可重试；未区分则写「任何错误」> | <有/无/不适用> | <文件路径，不带行号> |

### 应有约定建议

<指导性约定：哪些调用允许重试（幂等前提）、重试次数与退避上限、禁止无界重试、重试风暴防护（抖动）等；标注为建议>

## 三、熔断与降级

### 现状调用点分布

| 调用点（文件 · 函数） | 被调目标 | 熔断器 | 降级逻辑 | 证据（文件路径） |
|---|---|---|---|---|
| <文件 · 函数> | <服务/中间件/topic> | <hystrix / sentinel / resilience4j / 自研 / 无> | <fallback / 默认值返回 / 缓存兜底 / 直接失败> | <文件路径，不带行号> |

### 应有约定建议

<指导性约定：哪些被调目标应加熔断、熔断触发与恢复口径、降级返回的业务可接受性要求等；标注为建议>

## 四、panic/recover 与异常兜底

### 现状点位分布

| 点位（文件 · 函数） | 点位类型 | 兜底方式 | 证据（文件路径） |
|---|---|---|---|
| <文件 · 函数> | <出站调用 / 定时任务 / goroutine·线程 / 消费循环 / job> | <有 recover 兜底（位置）/ 顶层框架兜底 / 无兜底> | <文件路径，不带行号> |

### 应有约定建议

<指导性约定：每个独立执行单元（goroutine/线程/任务）必须有 recover 兜底、兜底后应记日志并告警、禁止 recover 后静默吞掉等；标注为建议>

## 五、错误 swallowing

### 现状点位分布

| 点位（文件 · 函数） | swallowing 形态 | 影响说明 | 证据（文件路径） |
|---|---|---|---|
| <文件 · 函数> | <赋值给 `_` / 空 catch / 只记日志不上抛不处理> | <该吞错可能掩盖什么故障> | <文件路径，不带行号> |

### 应有约定建议

<指导性约定：error 必须上抛或显式处理、记日志不等于处理、确需忽略的错误应注释说明理由等；标注为建议>

<!-- 模式缺省回退时，在文末追加一行：规范未建，本次为现状提取。 -->
