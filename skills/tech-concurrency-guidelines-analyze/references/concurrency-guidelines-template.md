# <池/原语实例名> 并发规范

| 元信息 | 值 |
|--------|-----|
| 分支 | <分支名> 分支 (<YYYY-MM-DD>) |
| 更新日期 | <YYYY-MM-DD> |
| Skill | tech-concurrency-guidelines-analyze |
| 运行模式 | 起草模式 |
| 实例类型 | <线程池 / 裸并发(goroutine/线程) / 锁 / channel / Actor / 定时任务> |

## 用途定位

<该池/原语实例在系统中承担什么角色：处理什么业务任务、为什么需要并发；从定义处注释/提交点所在函数/模块推断，推断不出标「待确认」>

- 代码标识符：<变量名 / 类型名 / 配置 key>
- 定义位置：<文件路径，不带行号>
- 使用点（任务提交 / 加锁 / 消息投递位置）：<文件路径（函数）；多处全部列出>

## 线程模型（可选）

<!-- 仅当有助于理解时画 mermaid：任务提交方 → 队列 → 工作线程 → 下游；label 一律加引号。不画则整节删除。 -->

```mermaid
flowchart LR
    submit["任务提交方"] --> queue["任务队列"]
    queue --> worker["工作线程池"]
    worker --> down["下游处理"]
```

## 容量 / 队列 / 拒绝策略现状（代码现状）

> 本节只写**代码事实**，逐条附证据文件路径（不带行号）；读不到写「未设置」或「框架默认」，禁止臆造取值。

| 维度 | 现状 | 证据（文件路径） |
|---|---|---|
| 池化方式 | <统一线程池 / 自研池 / 池化库 / 裸 goroutine/裸线程（无池化）> | <文件路径> |
| 容量配置 | <核心/最大线程数、取值来源（硬编码 / 配置 key）；读不到写「未设置」> | <文件路径> |
| 任务队列 | <队列类型与容量（有界/无界）、channel buffer 大小；无队列写「无队列」> | <文件路径> |
| 拒绝策略 | <CallerRuns / Abort / Discard / 阻塞等待 / 自研策略；无显式策略写「未设置（框架默认）」> | <文件路径> |
| 隔离范围 | <独占池 / 与哪些业务共用池；锁实例写保护对象> | <文件路径> |
| 关闭与等待 | <shutdown / 等待退出 / 超时取消（context、WaitGroup 等）；无则写「未设置」> | <文件路径> |

<!-- 实例类型变体：
- 锁实例：将「池化方式 / 容量配置 / 任务队列 / 拒绝策略」四行替换为——锁类型（Mutex/RWMutex）、锁粒度、锁顺序约定、临界区说明；
- 定时任务实例：补充「调度并发语义」行（是否允许重入、任务体内是否再开并发）；
- channel / Actor 实例：补充「消息缓冲与背压」行（buffer 容量、满时行为、mailbox 策略）。
不适用维度写「未设置」，不删除行。 -->

## 应有约定建议（建议）

> 本节为**建议**（规范初稿，尚未在代码中落地），与上节「代码现状」严格区分；落地后相应内容转入现状节、从本节移除。

| 维度 | 建议约定 | 理由 |
|---|---|---|
| 池选型 | <该场景应池化还是允许裸并发、应独占池还是共用池> | <一句话> |
| 容量基线 | <建议的核心/最大线程数推导思路（CPU 密集型 / IO 密集型）、队列有界化建议> | <一句话> |
| 拒绝策略 | <建议的拒绝/降级路径（拒绝后如何处理任务）> | <一句话> |
| 隔离 | <建议的池隔离边界（按业务 / 按优先级），避免相互打满> | <一句话> |
| 命名与观测 | <线程名/goroutine 标识规范、池水位与拒绝次数指标上报建议> | <一句话> |

<!-- 模式缺省回退（用户本意差距分析但无规范可对照）时，在本文档末尾追加一行：规范未建，本次为现状起草。 -->
