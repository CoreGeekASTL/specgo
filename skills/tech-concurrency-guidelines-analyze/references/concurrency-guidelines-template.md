# <池/原语实例名> 并发规范

| 元信息 | 值 |
|--------|-----|
| 分支 | <分支名> 分支 (<YYYY-MM-DD>) |
| 更新日期 | <YYYY-MM-DD> |
| Skill | tech-concurrency-guidelines-analyze |
| 实例类型 | <线程池 / 裸并发(goroutine/线程) / 锁 / channel / Actor / 定时任务> |

<!-- 章节上限三节：用途定位 + 使用说明 + 代码案例（元信息表不计章节），不多设章节。 -->

## 用途定位

<1~3 句人话：该实例处理什么业务任务、为什么需要并发/并发保护；从定义处注释/提交点所在函数/模块推断，推断不出标「待确认」>。例：鉴权缓存 map 的并发保护——鉴权链路高频并发读缓存、回源后写缓存并惰性清理，用 RWMutex 读写锁保护 items map（读多写少）。

## 使用说明

<!-- 调用入口清单：优先列封装层对外函数；无封装时列原语本身（加锁/提交/投递方法）与关键配置。定义文件不带行号。读不到显式配置写「未设置」或「框架默认」。 -->

| 函数 / 原语 / 配置项 | 作用 | 参数 / 取值说明 | 定义文件 |
|---|---|---|---|
| <authCache.Get(key string)> | <读缓存（RWMutex 读锁）> | <key：联合键 imei_imsi；未过期才返回> | <src/service/auth_cache.go> |
| <authCache.Set(key string, v bool)> | <写缓存（写锁内含超容清理）> | <容量 1000、单次清理 500，包级常量> | <src/service/auth_cache.go> |
| <……> | <一句话作用> | <逐参数 / 取值说明> | <文件路径> |

## 代码案例

<!-- 从业务代码摘取真实调用片段，照抄即可用；每段注明来源文件路径（不带行号）；典型读/写/提交场景各取一段代表性案例。 -->

```<lang>
// 来源：<文件路径>
<真实调用代码片段——场景 1：如读路径>
```

```<lang>
// 来源：<文件路径>
<真实调用代码片段——场景 2：如写路径>
```
