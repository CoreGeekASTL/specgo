# <服务名> 通信规范

## 接口清单（可选，章节较多时在文档开头加）

| 接口名 | 协议 | 调用位置 | 业务场景 |
|---|---|---|---|
| <接口名> | <协议> | <文件（函数）> | <一句话> |

## HTTP

### <接口名：方法+路径>

- 业务场景：<什么业务流程中发起该调用，从调用点所在函数/模块/注释推断；推断不出标「待确认」>
- 接口功能：<该调用完成什么功能，请求什么、返回什么>
- 调用位置：<文件（函数）；同一接口多处调用时列出全部文件>
- 协议信息：
  - 协议：HTTP POST /auth/v1/authIMEI
  - 封装方式：<直连裸 client / 统一封装层（封装层文件路径）/ 网关代理>
  - 超时重试：<显式超时取值与来源；重试次数与退避；无则写「未设置」或「框架默认」>
  - 错误码处理：<响应错误码 / 异常的分类处理现状；无显式处理写「未设置」>

## <RPC / MQ / IPC>

### <RPC 方法 / topic / IPC 方式名>

- 业务场景：<同上>
- 接口功能：<同上>
- 调用位置：<文件（函数）>
- 协议信息：
  - 协议：<gRPC 方法 / MQ topic / IPC 方式>
  - 封装方式：<IDL stub 直连 / 统一 client 封装（文件路径）>
  - 超时重试：<同上>
  - 错误码处理：<同上>
