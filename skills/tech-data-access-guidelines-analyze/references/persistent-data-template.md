# {存储名} 数据访问规范（持久化数据）

| 元信息 | 值 |
|--------|-----|
| 分支 | {分支名} 分支 ({YYYY-MM-DD}) |
| 更新日期 | {YYYY-MM-DD} |
| Skill | tech-data-access-guidelines-analyze |
| 运行模式 | 起草模式 |
| 数据分类 | 持久化数据（{关系库 / 嵌入式存储 / 对象存储 / 文件系统}） |

<!-- 写作红线（全篇适用）：
1. 全文只有两章——「数据库说明」与「数据库使用示例」，禁止增设其他章节。
2. 一句一事实——每句只讲一个事实，禁止用「；」「——」串联多个事实的复合长句；单句超 60 字必须拆分。
3. 说明章一律用表格或 bullet 列表呈现，禁止整段散文。
4. 证据句尾——证据文件路径（不带行号）放该句/该行末尾的括号内，不与事实混排在句中。
5. 示例驱动——全篇选一个代表性业务对象（一张表 / 一类对象）作贯穿示例，其余同类访问只归纳机制、不逐一枚举；全量表/字段清单归 docs/biz/data-model/，本资产不罗列。 -->

## 数据库说明

**{一句话定位：本存储是 XX 业务的权威数据源 / 归档存储}**（证据：{文件路径}）

连接与客户端管理：

- {连接链路一句：驱动、ORM 注册、代理层}（证据：{文件路径}）
- {client 形态（单例/每请求新建）与初始化点，一句}（证据：{文件路径}）
- {连接池参数取值与来源；未显式设置写「框架默认」}（证据：{文件路径}）
- {健康检查 / 重连 / 主备切换 / 关闭点，一句}（证据：{文件路径}）

可调用封装清单：

| 函数 / 入口 | 作用 | 来源文件 |
| --- | --- | --- |
| {List / Get / Insert / Update / Delete} | {通用 CRUD（ORM 参数绑定）} | {src/dao/base_dao.go} |
| {DoTxWithCtx} | {事务入口：回调注入 TxOrmer，返回 error 即回滚} | {src/dao/base_dao.go} |
| {InsertMultiWithOrm} | {批量插入，分批 N/批} | {src/dao/base_dao.go} |
| {ExecWithOrm / QueryOne / QueryMulti} | {Raw SQL 出口（参数绑定）} | {src/dao/base_dao.go} |

错误处理要点：

- {not found / 连接失败 / 唯一键冲突的处理方式，一句一条}（证据：{文件路径}）
- {重试与降级策略，一句一条；无重试如实写}（证据：{文件路径}）

风险点（可选，无则删除本小节）：

- {敏感信息泄露 / 无闸口参数等风险，一句一条，附文件路径}

## 数据库使用示例

以 {代表表}（选机制要素最全的一张——有索引 / 有事务写 / 有缓存联动者佳）为例，给出真实代码案例；其余同类实体的访问方式与其一致。

### 示例 1：{定义 DAO——封装层注入实体类型}

```go
{真实代码片段}
```

来源：{文件路径}

### 示例 2：{条件查询——QueryOption 参数绑定}

```go
{真实代码片段}
```

来源：{文件路径}

### 示例 3：{事务覆盖写——DoTxWithCtx + 清表 + 批量插入}

```go
{真实代码片段}
```

来源：{文件路径}

<!-- 示例按需 2~4 个，覆盖该存储最典型的读 / 写 / 事务路径；代码片段从仓内真实文件原样摘录，禁止改写；每例注明来源文件。 -->

<!-- 模式缺省回退时，在文末追加一行：规范未建，本次为现状盘点与约定起草。 -->
