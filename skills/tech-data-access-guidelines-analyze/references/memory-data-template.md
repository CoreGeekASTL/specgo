# {存储名} 数据访问规范（内存数据）

| 元信息 | 值 |
|--------|-----|
| 分支 | {分支名} 分支 ({YYYY-MM-DD}) |
| 更新日期 | {YYYY-MM-DD} |
| Skill | tech-data-access-guidelines-analyze |
| 运行模式 | 起草模式 |
| 数据分类 | 内存数据（{进程内内存结构 / Redis / Memcached}） |

<!-- 写作红线（全篇适用）：
1. 全文只有两章——「数据说明」与「数据使用示例」，禁止增设其他章节。
2. 一句一事实——每句只讲一个事实，禁止用「；」「——」串联多个事实的复合长句；单句超 60 字必须拆分。
3. 说明章一律用表格或 bullet 列表呈现，禁止整段散文。
4. 证据句尾——证据文件路径（不带行号）放该句/该行末尾的括号内，不与事实混排在句中。
5. 示例驱动——全篇选代表性业务对象作贯穿示例，其余同类访问只归纳机制、不逐一枚举。 -->

## 数据说明

**{一句话定位：本存储是 XX 的读缓存 / 会话态 / 计算中间态}**（证据：{文件路径}）

数据设计：

| 项 | 取值 | 证据 |
| --- | --- | --- |
| key 形态 | {进程内 map key / Redis key 命名模式} | {文件路径} |
| 值结构 | {结构体字段 / Redis value 类型} | {文件路径} |
| 容量上限 | {取值与来源（配置 key / 硬编码）；无上限如实写} | {文件路径} |
| TTL | {取值与来源；无 TTL 如实写} | {文件路径} |
| 清理策略 | {惰性清理 / 定期清理 / 容量驱逐} | {文件路径} |

读写与并发：

- 读写策略：{Cache-Aside / Read-Through / 进程内直读直写，一句}（证据：{文件路径}）
- 并发控制：{锁类型与粒度、清理与写入是否同锁，一句}（证据：{文件路径}）
- 持久层故障降级：{放行 / 拒绝 / 用旧值，一句}（证据：{文件路径}）

可调用封装清单：

| 函数 / 入口 | 作用 | 来源文件 |
| --- | --- | --- |
| {Get} | {查缓存，miss 返回什么} | {文件路径} |
| {Set / Add} | {写缓存，TTL 取值} | {文件路径} |

错误处理要点：

- {穿透 / 击穿 / 雪崩防护与失败兜底，一句一条；未设置如实写}（证据：{文件路径}）

## 数据使用示例

以 {代表业务对象} 为例，给出真实代码案例。

### 示例 1：{读路径——命中直接用 / miss 回源回写}

```go
{真实代码片段}
```

来源：{文件路径}

### 示例 2：{写路径——回源成功后回写缓存}

```go
{真实代码片段}
```

来源：{文件路径}

<!-- 示例按需 2~4 个，覆盖该存储最典型的读 / 写 / 清理路径；代码片段从仓内真实文件原样摘录，禁止改写；每例注明来源文件。 -->

<!-- 模式缺省回退时，在文末追加一行：规范未建，本次为现状盘点与约定起草。 -->
