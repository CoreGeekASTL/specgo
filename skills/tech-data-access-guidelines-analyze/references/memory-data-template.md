# {存储名} 数据访问规范（内存数据）

| 元信息 | 值 |
|--------|-----|
| 分支 | {分支名} 分支 ({YYYY-MM-DD}) |
| 更新日期 | {YYYY-MM-DD} |
| Skill | tech-data-access-guidelines-analyze |
| 运行模式 | 起草模式 |
| 数据分类 | 内存数据（{进程内内存结构 / Redis / Memcached}） |

## 数据设计与定位

### 现状

{存什么数据：key 形态与值结构（进程内结构体的字段 / Redis key 命名与 value 类型）；为什么放内存而不持久化——定位是缓存 / 会话态 / 计算中间态；容量上限与 TTL 取值及来源（配置 key / 硬编码）；数据生命周期：写入触发点、失效机制、清理策略（惰性清理/定期清理/容量驱逐）；附证据文件路径}。例：鉴权结论进程内缓存——map[imei_imsi]result，容量 1000 条、TTL 30min，超限按 expireAt 升序惰性清理最旧 500 条，定位为 DB 白名单的读缓存（证据：src/service/auth_cache.go）。

### 约定（起草待评审）

{从现状归纳的应有规则，如：进程内缓存必须显式容量上限与 TTL，禁止无限增长；key 统一 {业务域}:{实体}:{id} 格式；写路径必须先写持久层再失效缓存}

## 使用方式

### 现状

{读写策略（Cache-Aside / Read-Through / Write-Through / 进程内直读直写）；读写顺序与回写路径（miss 后回源谁、回写谁）；并发控制（锁类型与粒度、清理与写入是否同锁、有无独立 goroutine、single-flight）；与持久层的一致性路径（先写库后删缓存等）；持久层故障时的降级行为（放行/拒绝/用旧值）；附证据文件路径}

### 约定（起草待评审）

{如：读一律先缓存 miss 回源并回写；写路径禁止绕过封装层直接改 map；并发清理与写入必须同锁，禁止独立 goroutine 后台清理；持久层故障的降级策略须显式定义（fail-open / fail-close 二选一并注明理由）}

## 适用场景

| 业务场景 | 何时使用该数据 | 不适用时的兜底 |
| --- | --- | --- |
| {终端鉴权} | {每次鉴权先查缓存，命中未过期直接用} | {miss 回源 DB；DB 故障 fail-open 放行} |
| {……} | {从访问点上下文推断；推断不出标「待确认」} | {冷启动 / 容量驱逐 / 持久层故障时的路径} |

{说清分工边界：什么读写必须走该内存数据、什么场景禁止使用（如需要持久保证的写、跨实例共享的读——进程内内存不可跨实例）}

## 访问点分布

| 访问点 | 所在文件 | 所在函数 | 访问方式 | 业务场景 |
| --- | --- | --- | --- | --- |
| {鉴权结论查询} | {src/service/auth_service.go} | {AuthIMEI} | {封装层：src/service/auth_cache.go} | {鉴权时查询缓存结论} |
| {……} | {文件路径，不带行号} | {函数名} | {封装层（附路径）/ 裸 client} | {推断不出标「待确认」} |

## 错误处理

### 现状

{读写失败分类处理；穿透防护（空值缓存/布隆过滤）、击穿防护（互斥重建/逻辑过期）、雪崩防护（TTL 抖动）；失败兜底；附证据文件路径；读不到显式处理写「未设置」或「框架默认」}

### 约定（起草待评审）

{如：空结果短 TTL 回写防穿透；所有 key 显式 TTL 并加随机抖动；回源失败不长时间阻塞，按降级策略快速返回}

## 附注（可选，无此类情况整节删除）

- {预留死代码：封装存在但无业务调用方，逐条列出，附文件路径}
- {配置声明 vs 实际访问差异：配置里声明了但代码无实际访问的存储，逐条列出，附文件路径}

<!-- 模式缺省回退时，在文末追加一行：规范未建，本次为现状盘点与约定起草。 -->
