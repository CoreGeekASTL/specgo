# 基础规范（日志 / 配置 / 告警）

| 元信息 | 值 |
|--------|-----|
| 分支 | <分支名> 分支 (<YYYY-MM-DD>) |
| 更新日期 | <YYYY-MM-DD> |
| Skill | tech-foundation-guidelines-analyze |
| 运行模式 | 起草模式 |

## 机制全景

| 机制 | 使用框架 / SDK | 调用点分布概要 | 现状要点 |
|---|---|---|---|
| 日志 | <日志库 / 封装层，附文件路径> | <主要模块 / 目录，调用点数> | <一句话现状：级别使用 / 脱敏 / 审计概况> |
| 配置 | <配置文件 / 配置中心 / 环境变量读取入口> | <主要模块 / 目录，调用点数> | <一句话现状：读取方式 / 默认值 / 环境变量概况> |
| 告警 | <告警 SDK / 上报 client，附文件路径> | <主要模块 / 目录，调用点数> | <一句话现状：告警 ID / 上报恢复配对概况> |
| <增补机制> | <同上> | <同上> | <同上> |

<!-- 探测不到的核心机制在全景表中保留一行、现状要点列写「未发现」，证明排查过。 -->

## 日志

### 使用模式

<从代码归纳的用法骨架：logger 如何获取、典型打日志调用序列；业务代码走自有封装层时优先写封装层用法。真实代码片段注明来源文件路径（不带行号）。>

```<lang>
// 来源：<文件路径>
<代码骨架片段>
```

### 调用点分布

| 关注维度 | 分布（文件路径，不带行号） | 现状说明 |
|---|---|---|
| 级别使用 | <DEBUG / INFO / WARN / ERROR 调用点分布文件> | <各级别用于什么场景；有无级别滥用（如 ERROR 当 INFO 用、一律 INFO 不分级）> |
| 敏感信息脱敏 | <涉敏感信息日志所在文件> | <密码 / 密钥 / 证件号 / 手机号 / IMEI 等是否脱敏、脱敏方式；未脱敏的如实记录> |
| 审计日志 | <审计日志调用点文件> | <有无审计日志、记录哪些操作（登录 / 鉴权 / 数据变更）、格式与输出通道；无则写「未设置」> |

### 应有约定建议

| 编号 | 约定条目 | 现状符合度 |
|---|---|---|
| LOG-01 | <如：ERROR 级仅用于需要人工介入的异常，业务校验失败用 WARN，禁止级别滥用> | <现状已遵守 / 现状部分遵守 / 现状缺失> |
| LOG-02 | <如：日志中的敏感信息必须脱敏后输出，禁止明文打印密码 / 密钥 / 证件号> | <同上> |
| LOG-03 | <如：关键业务操作（登录、鉴权、数据变更）必须输出审计日志，含操作者 / 动作 / 结果> | <同上> |

## 配置

### 使用模式

<配置如何读取（配置文件 / 配置中心 / 环境变量的入口与封装）、结构体绑定与热更新方式。真实代码片段注明来源文件路径（不带行号）。>

```<lang>
// 来源：<文件路径>
<代码骨架片段>
```

### 调用点分布

| 关注维度 | 分布（文件路径，不带行号） | 现状说明 |
|---|---|---|
| 配置读取方式 | <各类读取入口所在文件> | <配置文件 / 配置中心 / 硬编码各自占比与场景；有无散落的硬编码配置> |
| 默认值处理 | <默认值处理点所在文件> | <缺配时的缺省值与行为；有无缺配即崩溃 / 静默取零值的情形> |
| 环境变量 | <环境变量读取点所在文件> | <哪些配置走环境变量、命名规律；有无环境变量与配置文件混用冲突> |

### 应有约定建议

| 编号 | 约定条目 | 现状符合度 |
|---|---|---|
| CONF-01 | <如：配置项必须有默认值，缺配时按默认值运行或启动失败并明确报错，禁止静默零值> | <现状已遵守 / 现状部分遵守 / 现状缺失> |
| CONF-02 | <如：环境变量仅用于部署差异项（地址 / 密钥引用），业务参数走配置文件 / 配置中心> | <同上> |
| CONF-03 | <如：新增配置项必须登记类型 / 默认值 / 含义，密钥类配置禁止入库明文> | <同上> |

## 告警

### 使用模式

<告警 ID 如何定义、告警如何上报与恢复；业务代码走自有封装层时优先写封装层用法。真实代码片段注明来源文件路径（不带行号）。>

```<lang>
// 来源：<文件路径>
<代码骨架片段>
```

### 调用点分布

| 关注维度 | 分布（文件路径，不带行号） | 现状说明 |
|---|---|---|
| 告警 ID 使用 | <告警 ID 定义与引用文件> | <集中常量 / 枚举定义还是散落魔法数、命名规律> |
| 上报与恢复配对 | <上报点与恢复点所在文件> | <每条上报是否有对应恢复、恢复条件；只报不恢复的逐条列出> |

### 应有约定建议

| 编号 | 约定条目 | 现状符合度 |
|---|---|---|
| ALM-01 | <如：告警 ID 集中定义、禁止散落魔法数，命名带模块前缀> | <现状已遵守 / 现状部分遵守 / 现状缺失> |
| ALM-02 | <如：可恢复故障的告警上报必须配对恢复上报，恢复条件与上报条件对称> | <同上> |
| ALM-03 | <如：告警内容必须携带定位所需上下文（模块 / 错误码 / 关键标识）> | <同上> |

## <增补机制（如监控埋点 metrics / 链路追踪 traceId 传递，按第 2 步探测结果添加；无则整节删除）>

### 使用模式

<同三核心机制：用法骨架 + 真实代码片段注明来源文件路径>

### 调用点分布

| 关注维度 | 分布（文件路径，不带行号） | 现状说明 |
|---|---|---|
| <该机制的关注维度> | <文件路径> | <现状说明> |

### 应有约定建议

| 编号 | 约定条目 | 现状符合度 |
|---|---|---|
| <PREFIX>-01 | <约定条目> | <现状已遵守 / 现状部分遵守 / 现状缺失> |

## 附注（可选，无此类情况整节删除）

- <排查到但无法判定归属 / 待人工确认的机制或调用点，逐条列出，附文件路径>

<!-- 模式缺省回退时，在文末追加一行：规范未建，本次为现状提取 + 约定建议。 -->
