# {仓名} 分支与变更规范（branch-guidelines）

| 元信息 | 值 |
|--------|-----|
| 适用仓库 | {仓名/路径} |
| 默认分支 | {默认分支名；判不出写「未识别（原因：xxx）」} |
| 更新日期 | {YYYY-MM-DD} |
| Skill | qual-branch-guidelines-analyze |
| 运行模式 | 起草模式 |
| 采样口径 | {分支：全量 N 条 / 按最近活跃采样 N 条；commit：{所在分支} 近 N 条} |

> 本规范为 guidelines 形态资产：指导性规范（"应该"遵守），违反出报告提示改进，不做 CI 拦截。
> 本文档为活文档，同名覆盖更新；各章「约定」小节均为**建议，待团队确认**，与「现状」事实分节呈现，不得混淆。

## 一、分支模型

### 现状

**分支命名形态分布**（证据：`git branch -a` 采样，口径见元信息）

| 命名形态 | 示例分支名（真实） | 样本数 | 占比 |
| --- | --- | --- | --- |
| {如 feature/{简述}，英文 kebab-case} | {feature/terminal-auth} | {N} | {%} |
| {如 无前缀、含工单号} | {12345-fix-login} | {N} | {%} |
| {……逐形态一行；形态从真实分支名归纳，禁止虚构示例} | {真实分支名} | {N} | {%} |

**长期分支与生命周期**：{常驻分支清单（如 develop / release / 集成分支）；已合入默认分支未删除 {N} 条、占采样 {%}；证据：git branch --merged 结果归纳}

**tag 与发布形态**：{语义化版本 v1.2.3 / 日期戳 / 无规律 / 无 tag；示例 tag 名 1~3 个（真实）}

### 约定（建议，待团队确认）

{从多数派实践归纳，如：分支命名统一 {type}/{工单}-{简述}（type ∈ feature / fix / hotfix / chore）；个人临时分支合入后即删；长期分支仅保留 {默认分支} 与 {集成分支}。现状分歧明显时给出收敛方向并注明现状分歧}

## 二、commit message 规范

### 现状

**类型前缀分布**（证据：近 {N} 条 commit 采样）

| 类型前缀 | 样本数 | 占比 | 证据（commit 短 hash） |
| --- | --- | --- | --- |
| {feat} | {N} | {%} | {a1b2c3d} |
| {无前缀} | {N} | {%} | {e4f5g6h、i7j8k9l} |
| {……逐形态一行} | {N} | {%} | {1~3 个真实短 hash} |

**语言分布**：中文 {N} 条（{%}）/ 英文 {N} 条（{%}）/ 混合 {N} 条（{%}）

**subject 长度分布**：≤50 字符 {N} 条 / 51~72 字符 {N} 条 / >72 字符 {N} 条

**引用形态**：{工单号 / MR 编号（如 "(#45)" 后缀）出现率 {%}；证据 commit 短 hash}

### 约定（建议，待团队确认）

{如：采用 conventional commits（type: subject）；subject ≤72 字符、祈使句；涉工单的变更 message 须带工单号；禁止无意义 message（如 "update"、"fix bug" 无上下文）}

## 三、merge 策略

### 现状

{判定结论：merge commit / squash / rebase / fast-forward / 多种并存；证据逐条列出——merge commit 短 hash 与 message 形态（如 "Merge pull request #N"）、线性历史 + "(#N)" 后缀（squash 痕迹）、线性且无 PR 引用（rebase 与 fast-forward 不可区分时如实注明）；多种并存给出各自占比与证据}

### 约定（建议，待团队确认）

{如：统一 squash merge 保持线性历史 / 统一 merge commit 保留功能分支轨迹；同一仓禁止策略混用}

## 四、MR 与评审要求

### 现状

**git 历史内评审痕迹**：{merge commit message 中 MR/PR 编号出现率；Reviewed-by / Approved-by / Signed-off-by trailer 出现率；证据 commit 短 hash}

**平台侧评审事实**：{gh / glab 可用时：平均评审人数、approval 数、是否要求 CI 通过；不可用时写「未识别（原因：无平台访问权限）」}

**仓内成文约定**：{CODEOWNERS / PR 模板 / CONTRIBUTING.md 中的评审要求要点摘录，附文件路径；无此类文件写「无」}

### 约定（建议，待团队确认）

{如：一切变更经 MR 合入、禁止直推默认分支；至少 1 人评审通过方可合入；CODEOWNERS 覆盖模块须属主会签}

## 附注（可选，无此类情况整节删除）

- {仓内 git 协作配置文件清单：CODEOWNERS / commitlint.config.* / .gitmessage / .husky/ / PR 模板，逐条附文件路径}
- {探测受限说明：浅克隆历史不全 / 无平台访问权限 / 无远端等}
- {异常样本剔除口径：机器人提交、批量历史迁移等不计入形态统计的样本，注明剔除依据}

<!-- 填写规则：现状小节只写从 git 历史与仓内文件读到的事实，证据为真实分支名 / commit 短 hash / tag 名 / 文件路径（不带行号）；约定小节从多数派实践归纳，行文即建议，不得与现状混淆；某维度探测不到时如实写「未识别（原因：xxx）」，不删除章节标题。模式缺省回退时，在文末追加一行：规范未建，本次为现状提取。 -->
