# {模块名} 模块结构文档

> 生成时间：{YYYY-MM-DD}
> 所属仓：{仓库名}
> 模块路径：{相对输入路径的路径}

## 模块职责

{2~4 句职责描述，基于目录名 + 抽样文件顶部注释 + 导出符号类型推断；读不到写"未识别"}

## 子模块关系图

{模块内第一层子包之间的 import 依赖图，mermaid flowchart，画法约定与总览一致：A 依赖 B 画成 `A --> B`，双向依赖画两条独立箭头，节点 label 一律加引号。只画模块内部子包之间的边；跨模块的边不画，跨模块关系统一由总览文档的模块说明表（依赖矩阵）承载，本模块文档不重复。退化规则（满足任一即不画图，保留标题并在标题下写一句说明）：
- 模块无子目录：写"本模块为扁平包结构，无子模块。"
- 模块仅 1 个子包：写"本模块仅 1 个子包 {子包名}，无子包间依赖。"
- 子包间无 import 依赖：写"子包间无 import 依赖关系。"}

## 子模块说明

{模块有子目录时，每个第一层子包一行（主要依赖/被依赖仅统计模块内其他子包，不含跨模块依赖）：}

| 子模块 | 路径 | 职责 | 主要依赖 | 被依赖 |
| --- | --- | --- | --- | --- |
| {子包名} | {相对输入路径的子包路径} | {1~2 句职责，基于包名 + 抽样文件顶部注释推断；读不到写"未识别"} | {模块内其他子包名，逗号分隔，无则"-"} | {模块内其他子包名，逗号分隔，无则"-"} |

{模块为扁平包结构（无子目录）时，上表替换为关键文件表：}

| 关键文件 | 说明 |
| --- | --- |
| {文件名} | {1 句说明，基于抽样} |
