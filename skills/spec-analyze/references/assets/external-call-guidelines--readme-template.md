# 通信规范（外部服务调用）

| 元信息 | 值 |
|--------|-----|
| 分支 | <分支名> 分支 (<YYYY-MM-DD>) |
| 更新日期 | <YYYY-MM-DD> |
| Skill | tech-external-call-guidelines-analyze |
| 运行模式 | 提取模式 |

## 外部服务全景

<!-- 可选：需要下游依赖全景图时，在表格前加 mermaid flowchart -->

| 服务名 | 协议 | 接口数 | 主要业务域 | 归属判定依据 | 子文档 |
|---|---|---|---|---|---|
| <服务名> | <协议> | <N> | <业务域> | <显式服务名 / 配置映射 / 上下文推断> | [external-call-guidelines-<service>.md](external-call-guidelines-<service>.md) |

## 附注（可选，无此类情况整节删除）

- <预留死代码：客户端封装存在但无业务调用方，逐条列出，附文件路径>
- <配置声明 vs 实际调用差异：依赖声明了但代码无实际调用的下游，逐条列出，附文件路径>

<!-- 模式缺省回退时，在文末追加一行：规范未建，本次为现状提取。 -->
