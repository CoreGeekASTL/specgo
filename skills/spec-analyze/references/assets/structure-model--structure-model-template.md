# {仓库名/目录名} 结构模型总览

> 生成时间：{YYYY-MM-DD}
> 分析模式：{仓库级 | 目录级}
> 输入路径：{相对或绝对路径}

## 概览

- 语言：{语言}
- 构建工具：{构建工具，未识别写"未识别"}
- 源码根：{路径，模式 B 写"输入目录本身"}
- 模块数：{N}
- 业务目录识别依据：{第 1~2 步的判定理由与排除说明}

## 模块关系图

```mermaid
flowchart LR
    moduleA["{模块A名}"] --> moduleB["{模块B名}"]
    moduleB["{模块B名}"] --> moduleC["{模块C名}"]
```

## 模块说明

| 模块 | 路径 | 职责 | 主要依赖 | 被依赖 |
| --- | --- | --- | --- | --- |
| {模块A} | {相对路径} | {2~4句职责} | {逗号分隔的模块名或"-"} | {逗号分隔的模块名或"-"} |
| ... | ... | ... | ... | ... |

## 分层特征

{2~4 句基于实证的分层观察：入口层/业务层/数据层流向是否单向、有无跨层直达、有无循环依赖；证据不足写"未识别"}
