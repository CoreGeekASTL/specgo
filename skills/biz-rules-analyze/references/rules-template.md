# {功能域中文名} 业务规则

> 生成时间：{YYYY-MM-DD}
> 覆盖入口：{该功能域入口清单，如 POST /auth/v1/login → controllers.AuthController.Login；多入口逐条列出}

## 概述

{1~3 句：该功能域解决什么业务问题、规则提取自哪些入口链路、覆盖哪几类规则点（条件分支/参数校验/状态迁移/阈值常量/错误码/事务回滚中的哪几类）}。例：登录功能域规则提取自登录与登出两条入口链路，含白名单校验、凭证校验两类条件分支、一组参数校验与两个业务错误码。

## 规则表

| 规则名 | 条件 | 动作 | 依据 | 来源入口 |
| --- | --- | --- | --- | --- |
| {check-white-list} | {请求 IMEI 不在白名单记录中} | {返回错误码 ErrNotInWhiteList，拒绝登录} | {src/service/auth_service.go} | {POST /auth/v1/login} |
| {max-login-retry} | {连续失败次数 ≥ MaxLoginRetry} | {锁定账号，返回错误码 ErrAccountLocked} | {src/service/auth_service.go；src/models/const.go} | {POST /auth/v1/login} |
| {order-status-paid} | {订单状态为 Pending 且支付回调验签通过} | {订单状态 Pending → Paid，触发发货流程} | {src/service/order_service.go} | {POST /pay/v1/callback} |

{表格写作规则：
- 规则名：从承载规则的代码标识符（函数名/常量名/错误码名/状态枚举名）派生转 kebab-case；同一标识符拆出多条规则时加短后缀区分；同篇文档内唯一；
- 条件：业务语言描述触发条件，保留关键代码表达式/常量名/枚举值；
- 动作：条件成立后的业务后果（返回错误码/拒绝请求/状态迁移/锁定/回滚/降级等），写清后果而非只写"返回错误"；
- 依据：文件路径，不带行号，多个用 ；分隔；
- 来源入口：该规则服务的对外入口（HTTP 填 方法 + 路径，消息订阅填 Consume topic-xxx，定时任务填 cron 表达式）；多入口共用同一规则时只列一行、来源入口逐条列全；
- 只收录有业务语义的规则点，纯技术防御分支（nil 判断、序列化失败等）不入表}

## 补充说明

{2~4 句：点明规则之间的优先级/互斥关系——如校验执行顺序、先拦哪个后拦哪个、状态迁移是否可逆、是否存在短路规则；有跨入口共用规则时一句话交代；规则条数做取舍时交代取舍口径}
