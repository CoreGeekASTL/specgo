# 文档质量审核方案

> 对象：代码仓文档分**两类**，审核策略不同——
> **需求/功能设计文档**（面向尚未实现的功能，事实依据是需求本身 + 人工裁定）→ 以**多彩建模 + 断点澄清**为主；
> **四类资产文档**（docs/{arch,biz,tech,qual}/，面向存量代码事实）→ 以**表达质量 + 代码一致性**两维度审核为主。
> 承载工具：spec-requirement-audit skill（需求审核，看文档+看代码）与 asset-audit（四类资产质量审核，spec-analyze 子流程），两者由原 spec-audit 拆分而来。本文档是两者的审核口径总纲，规则细节以两者各自 references/ 为准。

---

## 一、两类对象与审核策略

| 对象 | 代表文档 | 事实依据 | 审核策略 | 承载 |
|---|---|---|---|---|
| 需求/功能设计文档 | Spec、需求设计文档、功能设计文档 | 需求本身 + 人工裁定 | 多彩建模翻译 → 三类断点扫描（表述质量/逻辑断裂/设计要素）→ ask-human 批量澄清，**澄清未清零不进编码** | spec-requirement-audit |
| 四类资产文档 | arch 结构/交互模型、biz 接口/规则/对象/数据模型/词典、tech 使用/通信/并发/数据访问/韧性/基础、qual 编码/DT/分支规范 + 各域索引 | 存量代码 | 维度一表达质量（人能读懂）+ 维度二代码一致性（文档与代码事实比对，重点） | asset-audit |

两类各自的底层规则文件（spec-requirement-audit/references/assets/ 与 spec-analyze/references/assets/asset-audit--*）：

| 规则文件 | 内容 | 用于 |
|---|---|---|
| color-modeling.md | 多彩建模方法论（四色元素判定/连接/建模步骤） | 场景 1 |
| requirement-quality-checklist.md | 需求表述质量 5 类（成分残缺/弱表述/术语失范/语义多解/带病放行）+ 弱词黑名单 | 场景 1 |
| gap-checklist.md | 逻辑断裂点 12 类 + 严重程度分级 + ask-human 规范 | 场景 1 |
| asset-expression-rules.md | 资产表达质量规则 E1-E5 | 场景 2 维度一 |
| code-consistency-rules.md | 资产代码一致性规则 C1-C6 + 结论三档 | 场景 2 维度二 |

---

## 二、场景 1：需求/功能设计文档审核（澄清驱动）

机制要点（详见 spec-requirement-audit SKILL.md 五阶段）：

1. **多彩建模**：把文档翻译成「事件-角色-实体-描述」因果链路图，逻辑断裂点在图上自然暴露。
2. **代码对照**：文档描述的既有行为/接口/规则/数据结构与代码事实比对，初步定位注入点/复用点；不一致计入断点清单（类别「文档与代码不符」）。
3. **三类断点一次扫描**：表述质量（5 类）+ 逻辑断裂点（断点 1-9）+ 设计要素（断点 10/11/12：时序图/验收用例/接口信息），与代码对照断点合并。
4. **澄清（双模式）**：默认 ask-human 批量提问（全部断点一次抛出、每问 2-4 个候选选项并附基于代码事实的推荐做法）；用户声明"以报告形式呈现"则输出待澄清报告等回复；循环追问至无新问题；断点按 🔴 阻塞 / 🟠 风险 / 🟡 提示分级。
5. **质量门禁**：全部断点有人工裁定才放行；带病继续记「仍存疑」并单独列出"不建议进入编码"。
6. **产出**：建模 HTML + 规范功能实现设计 md（均落盘源文档同目录，一次成型）；含 mermaid 产出物必须 validate-mermaid.mjs 全部 VALID；收尾输出不落盘审核报告。

---

## 三、场景 2：四类资产质量审核（两维度）

资产跟着代码走。审核只做两个维度，不打分、不定级，结论三档。

### 维度一：表达质量（文字表达连续，人能读懂）

| 规则 | 检什么 | 处置 |
|---|---|---|
| E1 句子完整 | 规则/约束语句缺行为主体、作用对象或触发条件 | 待修订建议 |
| E2 弱表述 | 命中弱词黑名单（尽量/及时/合理/快速……，仓内 docs/audit/weak-words.txt 可追加） | 待修订建议 |
| E3 术语一致 | 多词一义/一词多义；业务术语抽样比对 docs/biz/lexicon/ 词条 | 待修订建议 |
| E4 结构连续与导航闭环 | 章节引用指向真实章节；表格无空表半截表；索引条目 ↔ 实际文件双向闭环、无死链无游离 | 待修订建议 |
| E5 待确认集中登记 | 「待确认」「代码未体现」散落正文未汇总进清单节 | 待修订建议 |

### 维度二：代码一致性（重点，零容忍项失实即判）

| 规则 | 检什么 | 适用 | 处置 |
|---|---|---|---|
| C1 锚点反查 | 文档引用的文件路径/标识符逐条 grep 核实存在（每篇至少抽 5 个） | 全部资产 | **零容忍** |
| C2 接口一致 | 接口表「方法+路径」与代码路由注册点双向比对（文档有代码无 = 失实；代码有文档无 = 覆盖缺失） | biz/interface | **零容忍** |
| C3 数据结构一致 | 表名/字段与建表 SQL、ORM 注册比对 | biz/data-model | **零容忍** |
| C4 出站调用一致 | 声称的下游服务/协议与代码实际出站调用点抽样比对 | tech/external-call-guidelines | **零容忍** |
| C6 mermaid 校验 | validate-mermaid.mjs 全部 VALID | 全部资产 | **零容忍** |
| C5 覆盖留痕 | 元信息含扫描数/收录数/采样口径 | 全部资产 | 待修订 |

### 各资产类型的一致性核验重点

| 资产 | 与代码比对什么 |
|---|---|
| arch 结构模型 | 包/模块划分与依赖方向和真实目录、import 关系 |
| arch 交互模型 | 抽样 1-2 条时序链路对照代码走查主链路 |
| biz 接口 | 路由注册点双向比对（C2） |
| biz 业务规则 | 规则条目的出处文件与条件分支真实存在 |
| biz 对象模型 | 类/结构体名 grep 命中，聚合间引用方向与代码一致 |
| biz 数据模型 | 表名/字段与 DDL、ORM 注册比对（C3） |
| biz 词典 | 代码命名映射抽样 grep 命中 |
| tech 使用/通信/并发/数据访问/韧性/基础 | 调用点分布引用真实文件；出站调用比对（C4） |
| qual 规范类 | 「现状描述」节引用的代码证据真实存在 |
| 索引 | 导航闭环（E4） |

### 结论判定与处置闭环

| 结论 | 条件 | 处置 |
|---|---|---|
| ❌ 失实 | 任一零容忍项（C1/C2/C3/C4/C6）命中 | 重跑对应 analyze skill，或走 spec-update 增量刷新 |
| ⚠️ 待修订 | 无失实，但有表达质量检出 / 覆盖缺失 / C5 缺失 | 报告给改进建议，落实到具体章节 |
| ✅ 可信 | 两维度均无检出 | 无需动作 |

**铁律**：一致性核验必须真实执行 grep/文件比对/校验脚本，每条失实附证据（执行了什么、命没命中），禁止目测通过；审核只检出不修改被审文档正文。

---

## 四、报告与归档

- 审核范围三种模式：**指定单篇** / **增量**（默认——只审受 git 变更影响的资产：变更落在 docs/ 直接纳入，变更落在代码则按文档引用的文件路径反查交集）/ **全量**（四类资产全审）。
- 每篇被审文档一个报告：`docs/report/{文档相对 docs/ 路径}/{文档基名}-audit.md`（镜像目录结构，同名覆盖，四节：结论/表达质量检出/代码一致性核验/改进建议）。
- 总览：`docs/report/README.MD`（两节：总体结论含三档分布 + 逐篇明细表「文档 | 域 | 表达质量 | 代码一致性 | 结论 | 日期 | 报告链接」）。
- 单篇与增量审核走更新模式（只动被审文档的报告与对应 README 行）；全量审核走全量重建。

---

## 五、治理度量指标（目标：逐季下降）

| 指标 | 口径 | 观察什么 |
|---|---|---|
| 资产失实率 | ❌ 失实篇数 / 已审核篇数 | 文档-代码一致性趋势 |
| 锚点有效率 | C1 命中锚点数 / 抽样锚点总数 | 引用防腐化效果 |
| 表达检出密度 | E 规则命中数 / 篇 | 写作质量趋势 |
| 待确认闭环率 | 已确认消解数 / 待确认登记总数 | 悬而未决存量 |
| 刷新及时性 | 代码变更 → spec-update 刷新的时差 | 资产保鲜能力 |
| 资产建库覆盖率 | 已建资产类数 / 16 | 资产体系完备度（基线指标） |

---

## 六、落地映射（规则 → 承载工具）

| 规则/环节 | 承载 | 现状 |
|---|---|---|
| C6 mermaid 渲染校验 | mermaid-validate `scripts/validate-mermaid.mjs` | 已有，直接可用 |
| C1 锚点反查 / C2-C4 比对 | asset-audit 子流程执行（grep/文件比对），各 analyze 子流程内嵌自检为出处 | 子流程内嵌已有，独立脚本待建 |
| E3 术语命中基准 | biz-lexicon-analyze 产出的词典 | 词典已具备 |
| E4 导航闭环 | 各资产目录内 README（analyze 子流程维护）+ asset-audit 双向比对 | 已有（域/总索引自 v3.0 不再自动生成） |
| 场景 1 澄清 | spec-requirement-audit（多彩建模 + 代码对照 + 断点 + 澄清双模式） | 已有 |
| 失实修复 | spec-update（增量刷新）/ 重跑对应 analyze 子流程（重建） | 已有 |

---

## 七、一句话总结

**需求文档靠澄清：多彩建模暴露断点，批量提问澄清清零才进编码；四类资产靠比对：表达读得懂、锚点/接口/表结构/调用与代码对得上，失实即打回刷新。**
