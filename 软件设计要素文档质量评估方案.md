# MD 文档质量评估方案

> 对象：代码仓 docs/ 资产分**两大类**，评估策略不同——
> **A 类：功能设计 / Story 设计文档**（docs/storys/ 下功能设计 md、{功能名}-story.md、develop-task 等；需求侧，面向尚未实现的功能）→ 评估以**澄清 + 逻辑完备性判断**为主；
> **B 类：17 类软件资产要素文档**（arch 2 / biz 5 / tech 6 / qual 3 / 索引 1；存量侧，面向代码事实）→ 评估以 **Linter 底线 + 逻辑完备性判断**为主。
> 评估单元口径：B 类以**资产类型**为评估单元，多篇制资产（interface / lexicon / data-model 等）主文档必查 + 子文档抽样；A 类以**单篇需求文档**为评估单元。
> 管理边界：本方案只管 **Linter（提交期机器检出）** 与 **反诘+映射（交互期歧义消解）** 两个阶段；写作期模板与准入期门禁不在本文档管理范围。

---

## 一、两大类资产与差异化评估策略

| 大类 | 代表文档 | 面向 | 评估策略 | 主导阶段 |
|---|---|---|---|---|
| A 功能设计 / Story 设计 | docs/storys/ 功能设计 md、{功能名}-story.md、develop-task | 需求 → 设计增量，事实依据是需求文档 + 存量代码抽查 | **澄清**（歧义反诘消解，未清零不进评分）+ **逻辑完备性判断**（建模断点、要素三态、变更可追溯） | 反诘 + 映射 |
| B 资产要素（17 类） | arch/biz/tech/qual 各资产文档 + 索引 | 存量代码事实归纳 | **Linter 底线**（零容忍项机器检出）+ **逻辑完备性判断**（完备率/一致率/覆盖率专项维度） | Linter |

两类共用一套通用层规则（分轨适用，见二）；专项层维度各自成表（见三）。

---

## 二、通用层：写作质量规则（分轨适用，B 类权重 40% / A 类见四）

| 规则 | 适用轨 | 手段 | 处置 |
|---|---|---|---|
| 主语完整性：每条验收必须有明确行为主体 | A | Linter | **零容忍**，检出即不通过 |
| 宾语完整性：动作必须作用于明确对象 | A | Linter | **零容忍** |
| EARS 句式：触发条件必填 | A（表格化章节以「条件/动作/依据」列结构等价承载） | Linter | **零容忍** |
| 弱词黑名单：词表可配，检出即提示 | A | Linter | 扣分项，每处 −1 |
| 可测性：形容词强制改写为量化指标 | A | Linter | 扣分项，每处 −1 |
| 术语引用校验：术语必须命中领域词典 | A + B | Linter | **零容忍** |
| 术语唯一性：全局统一命名，禁止别名 | A + B | Linter | **零容忍** |
| mermaid 渲染校验：文档内图全部 VALID | A + B | Linter（validate-mermaid.mjs，已有） | **零容忍** |
| 待确认集中登记：「代码未体现，待确认」「需求未明确」标注必须汇总进清单节，禁止散落 | A + B | Linter | 扣分项，每处 −1 |
| 代码锚点有效率：引用的代码标识符与文件路径必须真实存在（grep 反查），证据不带行号 | B | Linter（待建脚本，现由各 skill 自检人肉执行） | **零容忍** |
| 链接与导航闭环：主文档导航 ↔ 目录文件一一对应，无死链、无游离文件 | B（A 类 story 索引同查） | Linter（待建脚本） | **零容忍** |
| 覆盖自检留痕：扫描数 / 收录数 / 差集说明（或采样口径）写入文档元信息或说明节 | B | Linter（检出留痕节存在性） | 扣分项，缺失 −2 |

> 通用分 = 100 − 扣分项累计；零容忍项不计分、检出即不通过。
> 覆盖自检留痕的现实样例：interface 主文档「扫描 36 个接口、差集清零」、韧性规范「扫描范围总览」、分支规范「采样口径」。

---

## 三、专项层：逻辑完备性判断（每维度 0–5 分）

### A 类：功能设计 / Story 设计（澄清 + 逻辑完备性）

**澄清质量**（反诘阶段产出）：

| 专项维度 | 度量判据 |
|---|---|
| 歧义消解闭环率 | 反诘问题全部有用户拍板结论，无残留带进设计/编码；无疑问时明确回复"无疑问" |
| 未明确标注合规率 | 未澄清点显式标注"需求未明确，待详设确认"，无脑补填充 |

**逻辑完备性**（专项评审）：

| 专项维度 | 度量判据 |
|---|---|
| 逻辑断点清零 | 多彩建模事件链每个粉色事件四要素（触发者/输入/输出/后继）齐全；角色/实体/规则无断点；无文件名/函数名泄漏进图 |
| 八要素判定完整率 | story 核心要素变更总览八行齐全，三态（新增/变更/不涉及）无遗漏判定 |
| 变更条目可追溯率 | 每个「变更」条目写清变更对象（既有要素+文件）与更改内容（变更前 → 变更后）；变更前状态有资产文档或存量代码事实依据 |
| 规划标注合规率 | 规划文件标"（规划）"，复用/变更点指向真实存量文件，无臆造路径 |
| 验收可执行性 | develop-task 修改文件清单到函数级、验证方式可运行；澄清问题列表逐条有结论 |

### B 类：资产要素（逻辑完备性判断）

**架构要素（定结构）**

| 文档 | 专项维度 | 度量判据 |
|---|---|---|
| 结构模型 | 依赖方向合规率；包覆盖率 | 工具扫描包依赖无反向；仓内所有包都在图中 |
| 交互模型 | 主链路代码一致率；链路枚举完备率 | 抽样 1–2 条链路走查一致；对外入口（路由/订阅/定时）均有对应链路文档。**异常分支不考核本文档**——按现行口径只画主链路，分支与异常归业务规则文档（见其「分支穷尽率」） |

**业务要素（定业务）**

| 文档 | 专项维度 | 度量判据 |
|---|---|---|
| 对象模型 | 聚合边界合理性；要素完备性；命名命中率；类图口径合规 | 无跨聚合直接引用；实体/值对象/服务/事件齐全；类名可查词典；只画聚合内结构与聚合间引用方向，未把 DB 表字段机械照抄成类图 |
| 数据模型 | 字段语义明确率；生命周期闭环；物理关联标注合规 | 每张表有属主、有创建/清理策略；内存态与持久态一致；逻辑关联无 DB 约束时标注"代码未体现物理外键" |
| 领域词典 | 术语覆盖率；一词一义；映射反查有效率；待确认闭环率；子域口径一致率 | 代码标识符命中词典；无同名异义（异义按语境分条）、同义异名已合并互指；代码命名映射抽样 grep 全命中；待确认项全部汇总主文档清单；子域与 interface 功能域一一对应（锚点一致） |
| 业务规则 | 分支穷尽率；依据可追溯率；来源入口覆盖率 | 正常/异常/边界分支齐全（EARS 条件-动作结构）；每条规则有出处（文件路径）且标注来源入口 |
| 接口 | 契约完整性；兼容性；与实现一致性；归集差集清零 | 参数/错误码/幂等齐全；错误码可定位到处理动作；路由注册扫描数=收录数（差集清零，双入口重复注册已去重说明） |

**技术要素（定用法）**

| 文档 | 专项维度 | 度量判据 |
|---|---|---|
| 框架使用现状 | 框架清单完备率；使用模式代码一致率 | 依赖清单（go.mod 等）逐项核对无遗漏；调用点分布引用真实文件路径 |
| 通信规范 | 超时重试参数齐全率；框架版本匹配；出站调用盘点完备率 | 每个出站调用有超时与降级策略；无过期写法；被调服务清单与代码扫描一致 |
| 并发规范 | 池参数有容量依据；无私建线程池；原语盘点完备率 | 与现网配置一致；扫描无私建（如裸 goroutine 未登记）；池/锁/channel 实例与代码扫描一致 |
| 数据访问规范 | 连接/超时配置一致性；访问点完备率 | 与中间件版本、部署配置一致；中间件访问点分布与代码扫描一致 |
| 韧性规范 | 出站调用覆盖率；故障策略留痕率 | 新增外部调用必过本规范逐条核对；超时/重试/熔断现状逐点有证据文件 |
| 基础规范 | 日志/告警例外有理由；敏感信息脱敏覆盖 | 例外项逐条登记；新增日志无明文密码/密钥/token |
| （规范类通用） | 双模式与分节合规 | 起草/差距分析模式标注正确；「现状描述」与「应有约定」分节，约定标注「建议，待团队确认」，现状与建议不混淆 |

**工程要素（定红线）**

| 文档 | 专项维度 | 度量判据 |
|---|---|---|
| 编码规范 | 规则可自动化率；例外豁免率 | 可交 linter 执行的条款占比逐季上升；豁免逐条有理由 |
| DT 规范 | 覆盖基线达成率；用例方法落地率 | 新增代码覆盖率达标；用例标注等价类/边界值 |
| 分支与变更规范 | 采样口径留痕；约定标注合规；差距报告闭环 | git 采样量与口径写入元信息；现状与约定分节、约定标注「建议，待团队确认」；差距报告红线单独成节可供 CI 解析 |

**索引（定导航）**

| 文档 | 专项维度 | 度量判据 |
|---|---|---|
| 索引 | 链接闭环率；聚合真实性；覆盖完备率 | 域索引/总索引无死链；只聚合真实存在的文件、不重述资产正文；全资产目录均已登记（taxonomy 外目录有附注说明） |

---

## 四、总分与分级

- **B 类**：总分 = 通用分 × 40% + 专项分 × 60%（专项分 = 各维度均值 / 5 × 100）。
- **A 类**：**澄清未清零不出分**——反诘问题有残留直接打回，不进评分；清零后总分 = 通用分 × 40% + 专项分 × 60%（通用分取 A 轨适用规则）。
- **≥ 90 基线归档**：进入 README 索引，状态"已基线"（状态位由 all-index 生成索引时记录）；
- **75–89 待修订**：限期内修订复评，状态"待评审"；
- **< 75 重写**：打回，归因记录进留档；
- A 级文档（功能设计/Story 设计、业务规则、对象模型、领域词典、数据模型、接口）：机器评分之上，**必须人审专项维度签字**才生效。

---

## 五、治理度量指标（目标：逐季下降）

| 指标 | 口径 | 观察什么 |
|---|---|---|
| 歧义检出率 | Lint 命中数 / 篇 | 写作质量趋势 |
| 反诘密度 | /clarify 选择题数 / 篇 | 表述歧义存量 |
| 澄清闭环率 | 拍板结论数 / 反诘问题总数（功能设计/Story） | A 类澄清充分度 |
| 返工归因占比 | 生成返工中归因"表述问题"的比例 | 评估体系有效性 |
| 文档-代码一致率 | 季度抽检代码锚点有效率（含 all-update 刷新及时性：变更后刷新时差） | 防腐化效果 |
| 待确认闭环率 | 已确认消解数 / 待确认登记总数 | 词典/规则/story 的悬而未决存量 |
| 资产建库覆盖率 | 已建资产类数 / 17（一次性基线，此后转保鲜指标） | 资产体系完备度 |

---

## 六、落地映射（规则/维度 → 承载工具与 skill）

| 阶段/规则 | 承载 | 现状 |
|---|---|---|
| Linter：mermaid 渲染校验 | spec-mermaid-diagram `scripts/validate-mermaid.mjs` | **已有，直接可用** |
| Linter：术语引用校验 | biz-lexicon-analyze 产出的词典为命中基准 | 待建 linter（词典已具备） |
| Linter：代码锚点反查 | 各 analyze skill 自检步骤（映射反查）人肉执行；grep 校验脚本 | skill 内嵌已有，独立脚本待建 |
| Linter：链接/导航闭环 | 各多篇制资产 skill 自检（导航闭环检查项） | skill 内嵌已有，独立脚本待建 |
| Linter：覆盖自检留痕 | 各 analyze skill 自检（interface 差集清零 / lexicon 五类来源 / resilience 扫描总览） | 已有实践，留痕节存在性可由 linter 检出 |
| 反诘：功能设计澄清 | spec-audit 场景 1（表述质量扫描 + 逻辑断点 + ask-human 批量澄清，原 spec-logic-audit 并入） | 已有 |
| 反诘：Story/编码澄清 | spec-audit 场景 2 A 轨（story 加查三项 + 澄清门禁）+ spec-story-design 5.2 + code-generate 编排确认门 | 已有 |

---

## 七、一句话总结

**两大类资产两条轨：功能设计/Story 先澄清清零、再评逻辑完备；17 类资产要素 Linter 管底线（术语、句式、图可渲染、锚点真实、链接闭环零容忍）、专项尺子量完备；分数进索引、归因促下降。**
